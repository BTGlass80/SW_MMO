.godot/
.import/
export/
*.tmp
*.log

# Raw asset archives: large (~900MB), re-fetchable via `python tools/fetch_assets.py kenney`.
# The curated, Godot-ready subset under assets/3d/ IS tracked.
MMO_Assets/

# Python caches
__pycache__/
*.pyc

# Claude Code runtime locks (keep tracked: .claude/agents/, CLAUDE.md)
.claude/scheduled_tasks.lock


# Soak / watchdog run artifacts (reports + per-run logs)
soak_logs/
watchdog_logs/
soak_report.txt
