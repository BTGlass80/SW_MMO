# Meshy Image to 3D Request
  ## Safety  The API key is read from `MESHY_API_KEY` and is not written to this folder. The request body stores a redacted image marker, not the full data URI.
  ## Safety  The API key is read from `MESHY_API_KEY` and is not written to this folder. The request body stores a redacted image marker, not the full data URI.Asset id: `meshy_image_segmented_droid_meshy5_geometry_v0`
  ## Safety  The API key is read from `MESHY_API_KEY` and is not written to this folder. The request body stores a redacted image marker, not the full data URI.Display name: Meshy Image Segmented Droid Meshy 5 Geometry Probe v0
  ## Safety  The API key is read from `MESHY_API_KEY` and is not written to this folder. The request body stores a redacted image marker, not the full data URI.Submitted: `True`
  ## Safety  The API key is read from `MESHY_API_KEY` and is not written to this folder. The request body stores a redacted image marker, not the full data URI.Source image: `docs/gpt/asset_factory/generated/meshy_image_droid_v0/source_reference/blockcraft_segmented_droid_source.png`
  ## Safety  The API key is read from `MESHY_API_KEY` and is not written to this folder. The request body stores a redacted image marker, not the full data URI.
  ## Safety  The API key is read from `MESHY_API_KEY` and is not written to this folder. The request body stores a redacted image marker, not the full data URI.## Purpose
  ## Safety  The API key is read from `MESHY_API_KEY` and is not written to this folder. The request body stores a redacted image marker, not the full data URI.
  ## Safety  The API key is read from `MESHY_API_KEY` and is not written to this folder. The request body stores a redacted image marker, not the full data URI.System.Object[]
