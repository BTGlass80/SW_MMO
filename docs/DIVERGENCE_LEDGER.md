# WEG / SW_MUSH / Prototype Divergence Ledger

Use this file whenever a mechanic differs across WEG, SW_MUSH, and this Godot prototype.

## Format

| ID | Area | WEG Source | SW_MUSH Behavior | Prototype Behavior | Reason | Status |
|---|---|---|---|---|---|---|

## Entries

| ID | Area | WEG Source | SW_MUSH Behavior | Prototype Behavior | Reason | Status |
|---|---|---|---|---|---|---|
| DIV-0001 | Ground location model | R&E assumes cinematic scenes and range/difficulty context | Rooms, exits, maps, zones | Continuous 3D settlement with interaction zones | Medium translation from text rooms to spatial play | Accepted translation |
| DIV-0002 | Combat presentation | Turn-based tabletop rounds | Turn-based command rounds | Server-authoritative rounds with real-time camera/animation wrapper | Preserve rules while making visual play responsive | Planned |
| DIV-0003 | Building/destruction | Not a voxel construction game | Text rooms, housing, cities, structures | Minecraft-styled visuals without general destructible voxels | User direction: visual style, not full voxel simulation | Accepted |
| DIV-0004 | Space map | WEG tactical/range bands and astrogation | Clone Wars zone graph plus tactical combat grid | Flat 2.5D plane with 3D ships/camera and zone graph overlay | Medium translation while preserving ship mechanics | Planned |
| DIV-0005 | Era handling | WEG R&E book framing is older-era by default, but mechanics are era portable | SW_MUSH still has some legacy old-era references | Prototype is Clone Wars only; old-era labels are invalid unless retranslated | User direction and current SW_MUSH launch direction | Accepted |
| DIV-0006 | Death / loot penalty | R&E: GM-discretion lethality, no loot rules | SW_MUSH: full-loot decaying corpse in unsafe zones, credits kept | Owner-decided NUMBERS (2026-07-01), wiring in Wave F via pure `death_penalty_model.gd`. On the 'dead' tier (reached only via hostile PvE in a lawless zone, DIV-0017): credits KEPT; each equipped item loses 10% durability (3% if insured; at 0 = 'broken'/halved pools until repaired); 50% of UNEQUIPPED inventory drops to a security-tier-decaying corpse (secured=instant restore, contested=2h, lawless=4h per Guide_19 §4); equipped weapon+armor never drop. INSURANCE = 500cr / 3 charges (`sheet.insurance`) → no-drop + 3% durability. RESPAWN = nearest secured med bay (`WorldState.SPAWN_POINT`), 10s blackout, wound_state='wounded'(sev 2) + the existing recovery_model post-death −1D DEATH_DEBUFF (6 rounds). The DIV-0016 sparring cap still bounds the training dummy; lifted ONLY for hostile-NPC encounters. **WIRED (Wave F S11-S13):** `_handle_player_death` runs the penalty + relocates to `WorldState.SPAWN_POINT` + default (secured) zone + writes `world_hooks.corpse`; two-process verified. v1 SIMPLIFICATIONS (tracked follow-ups): death fires on INCAPACITATION (live wound ≥ DISABLED_SEVERITY 3 from a lethal hit — a lone character taken out by a hostile is killed) rather than modeling the mortally-wounded death-roll grace or the 10s blackout; the −1D DEATH_DEBUFF and the durability=0 'broken'/halved-pools effect are tracked (respawn-wounded already applies −1D via the wound ladder); third-party corpse looting in lawless is a follow-up (manifest is written) | Owner direction 2026-06-24/07-01: mainstream MMO retention over hardcore full-loot | Accepted; WIRED Wave F (docs/UNLOCK_SYSTEMS_PLAN.md) |
| DIV-0007 | CP progression source/pace | R&E: CP awarded per session, spent to raise skills | SW_MUSH: kudos + optional LLM pose-grading, deliberately slow (~months/+2D) | Dual-track: fast gameplay-driven CP (quests/kills/exploration) + slow RP-prestige CP | Owner direction 2026-06-24: broad audience with an RP layer | Accepted |
| DIV-0008 | Wound ladder / penalty dice | R&E cumulative wound ladder w/ Wounded-Twice (-2D); Guide_19 §1 | named cumulative ladder | previously COLLAPSED: ground_combat _wound_penalty_dice returned 0D for the whole top of the ladder (silent loss of the cumulative Wounded-Twice -2D tier); now wound_ladder_model restores the per-level penalties faithfully — the ACTABLE tiers carry stunned/wounded -1D and **wounded_twice -2D**, while the "out" tiers (incapacitated/mortally_wounded/dead) carry **0D** per WEG canon (Guide_19 §1 "(out)/(bleeding), Can Act? no"; Guide_01 §7 L393 "0 for Incap/Mortal/Dead — moot, can't act") — and models full cumulative escalate(); wiring escalate() into live action-window accumulation is a tracked follow-up | restore WEG fidelity without destabilizing seeded combat smokes | Accepted |
| DIV-0009 | Recovery timing & heal roll | R&E/Guide_19: stun lifts after a few rounds; First Aid/Medicine roll (with Wild Die) vs wound-level difficulty drops one level; Mortally Wounded rolls 2D<rounds to die; post-death -1D for ~1 real-time hour | Guide_19 §3/§5 tables | recovery_model: stun auto-clears at 2 rounds; heal_check is a straight pool sum (NO Wild Die explosion) vs the Guide_19 §3 difficulty (8/11/14/16/21), success drops one level; death_roll 2D<rounds faithful; post-death -1D modeled as DEATH_DEBUFF_ROUNDS=6 combat rounds (round-keyed) instead of 1 real-time hour | server-authoritative round-keyed timers + deterministic recovery checks; real-time/bacta clearance + the Wild Die on heal rolls are presentation/tuning follow-ups | Accepted |
| DIV-0010 | Ranged range bands | R&E: each weapon has its own short/medium/long brackets with per-band difficulties (VE/E/M/D) | Per-weapon ranges in weapons.yaml | previously a SINGLE fixed RANGE_BANDS table (PB≤4/Short≤16/Medium≤32/…) approximated ALL weapons; now `d6_rules.range_band_for_weapon()` uses each weapon's own `[short_min,short_max,med_max,long_max]` (point-blank<short_min=VE5, Short=E10, Medium=M15, Long=D20, beyond=Extreme30) via an optional `weapon_ranges` arg threaded through `resolve_ranged_attack` (fixed table kept as the fallback; live ground-combat callers not yet passing weapon_ranges — a tracked wiring follow-up) | restore per-weapon range fidelity without breaking existing combat callers | Accepted |
| DIV-0011 | Force sensitivity / Jedi access | R&E: Control/Sense/Alter Force skills; force-sensitive chosen at creation; Jedi/Force users | SW_MUSH has a Force access/scarcity policy; chargen-time force_sensitive flag | ACCESS POLICY DECIDED (owner 2026-07-01, "the SWG Village solution"): force_sensitive is NOT chargen-selectable and NOT open — it is EARNED via a hidden, rare, multi-phase 'awakening' questline modeled on SWG's Village of Aurilia, adapted to Clone Wars 20 BBY (underground Force-latents, not the open Jedi Order). `force_awakening_model.gd` (pure, wiring in Wave F) owns a 4-phase (DORMANT→1..4→COMPLETE) achievement gate advanced ONLY by deterministic server signals (lifetime CP, distinct zones, combat disables, First-Aid heals given, wound survivals, tense-zone participation) + a server-owned rare manifest roll (~2%/Director-tick) behind a hard prerequisite + a server-wide soft cap (~8 awakened+in-progress). On Phase-4 completion `apply_completion` flips `sheet.force_sensitive=true` and activates the off-by-default `force_skills_model`. Progress persists in `sheet.force_unlock`. Force POWER list + Dark Side Points remain owner-gated/unimplemented | land the earned-access + scarcity policy per owner direction while keeping Force-users rare and the power economy owner-gated | Accepted (access decided; questline wiring in Wave F) |
| DIV-0012 | Natural wound recovery (live wiring of DIV-0009) | R&E/Guide_19 §3: a wounded character recovers over rest time; a Strength roll (or First Aid/Medicine by another) vs a wound-level difficulty drops the wound one level | Guide_19 §3 heal table | `recovery_model` (DIV-0009) is now WIRED into the live server loop: on each Director tick (~30s = one recovery interval, server-owned `_server_rng`) every CONNECTED character whose persisted `sheet.wound_state` is a "can still act" tier (stunned / wounded / wounded_twice) makes a NATURAL self-recovery `heal_check` using the character's OWN Strength pool vs the Guide_19 §3 difficulty (8/11/14); on success the wound drops exactly one level, is persisted, and refreshes the live combat penalty. INCAPACITATED / MORTALLY_WOUNDED / DEAD are EXCLUDED from self-recovery (they need First Aid/Medicine by another — a later slice — and the lethal tiers are owner-gated death, DIV-0006). No death roll is auto-run. Heal pool = own Strength is an approximation of WEG rest-recovery; the ~30s interval is a tunable default (round-keyed per DIV-0009), NOT an owner balance ruling | close the persistent wound→recovery loop (without it, any wound is permanent); reuse the already-accepted DIV-0009 model; keep the owner-gated death/medical paths out | Accepted |
| DIV-0013 | First Aid / Medicine recovery by another (medic heals an ally) | R&E/Guide_19 §3: a character applies First Aid (Technical) or Medicine vs the target's wound-level difficulty; success drops the wound ONE level; a given wound may be First-Aided once per healer (no infinite retries on a single failed attempt) | Guide_19 §3 "Getting Healed" table | `submit_heal(target_id)` RPC: a healer in the SAME zone treats another CONNECTED player (never self) with a `Recovery.heal_check` using the healer's First Aid pool (Technical attribute + `first_aid` skill bonus) vs the Guide_19 §3 difficulty (server-owned `_server_rng`); on success the target's wound drops one level, is persisted, and refreshes the target's live combat penalty (`set_player_combat` merges only `player_wound_severity`). UNLIKE natural self-recovery (DIV-0012) this CAN treat `incapacitated` and STABILIZE `mortally_wounded` (drop toward `incapacitated`); `healthy` (no_wound) and `dead` (beyond_help) are rejected; NO death roll / death PENALTY is involved (owner-gated DIV-0006, still unwired). Retry restriction is approximated PER-TARGET (a wound can be First-Aided once until its level CHANGES — by a successful heal, a new hit, or a natural-recovery tick) rather than WEG's strict per-healer-per-wound; First Aid uses a straight pool sum (no Wild Die) per DIV-0009 | make wounds cooperatively treatable (esp. the incapacitated/mortally tiers self-recovery can't reach) without touching the owner-gated death system; cooperative healing is not PvP-consent territory | Accepted |
| DIV-0014 | Inter-zone travel | MUSH: spatial room graph with named exits (move room-to-room); WEG has no zone-travel rule (it is a MUD/MUSH world-model concern) | SW_MUSH room/exit graph + zone tags | extends DIV-0001's room→zone translation: the 4 Mos Eisley zones (`data/zones_clone_wars.json`) have NO spatial connection or seamless streaming, so a `submit_change_zone(zone_id)` RPC provides COMMAND fast-travel to any loaded zone (no adjacency/route restriction modeled yet). The server updates the peer's zone (so snapshot routing, zone-scoped chat, ambient roster, and the territory view all follow), persists it on the record (restored on next login), and replies. Travel is unrestricted + instant (no travel time, fuel, fatigue, or distance cost) | exercise the already-built per-zone systems in a single session (without travel the multi-zone world is only reachable by reconnecting with `--zone`); adjacency/route/cost restrictions are a later design layer, owner-tunable | Accepted |
| DIV-0015 | Per-species movement speed | R&E/MUSH: each species has a base Move rate in METERS/ROUND (e.g. human 10, wookiee 11, mon_calamari 9) | `data/species_clone_wars.json` `move` field | `world_state` previously moved ALL players at one fixed real-time `MOVE_SPEED = 6.5` (Godot units/sec, matching the solo `PlayerController`), ignoring species. Now the server sets each player's real-time speed at login = `MOVE_SPEED × (species_move / DerivedStats.DEFAULT_MOVE[=10])` via the (previously orphaned) `derived_stats_model.move_for_species`, so the canonical RELATIVE differences carry over (wookiee ×1.1 → 7.15, mon_calamari ×0.9 → 5.85, all move-10 species unchanged at 6.5). The m/round→real-time conversion is a ratio-to-default, NOT a literal unit conversion; the absolute baseline (6.5) is unchanged so existing feel/tests for move-10 species are untouched | make species choice mechanically meaningful for movement (faithful) while preserving the tuned baseline; wire the built-but-orphaned derived-stats model | Accepted |
| DIV-0016 | Non-lethal sparring damage (training-target return fire) | R&E: combat is lethal at GM discretion (return fire can incapacitate/kill); STUN damage (stun setting / training remotes) is non-lethal — knockout, no kill; a GM may declare a subdual/sparring bout bounded to a chosen wound tier; Guide_19 §1 wound ladder | SW_MUSH: combat damage is real/lethal in unsafe zones; training rooms / safe-zone subdual exist | The SHARED `b1_training_silhouette` training target DOES return fire under FULL WEG resolution (server-owned RNG; the complete combat envelope/events still carry the TRUE rolled severity for auditability). Two INDEPENDENT guards keep it non-lethal: (1) `ground_combat_model._resolve_return_fire`'s `stun_mode` is now DATA-DRIVEN via the target's `stun_return_fire` field (DEFAULT true = pure WEG stun, sev≤1, UNCHANGED for every other target/path) — the sparring remote sets it false so return fire can roll a real Wounded(2); (2) `combat_arena.resolve_window` then CLAMPS the player's resulting `player_wound_severity` at `SPARRING_MAX_SEVERITY = 2` (Wounded) via `mini()` (cap only LOWERS, never heals), so the trainee climbs healthy→stunned(1)→wounded(2) and STOPS — NEVER incapacitated(3+). The model produces a faithful severity; the arena bounds it. All resulting wounds sit in the HEALABLE band already served by natural self-recovery (DIV-0012) + First Aid (DIV-0013). The pure rules layer is otherwise untouched (wound ladder DIV-0008 + recovery DIV-0009 unchanged); the lethal ceiling lives ONLY in the server-side arena orchestrator. PvP / non-training NPCs are explicitly OUT of scope — the cap is keyed to the training-target arena, and real-stakes lethal combat remains owner-gated (DIV-0006). Per-hit severity 2 = 'wounded'; the cumulative 'wounded_twice' ladder tier (DIV-0008) is not wired into live accumulation, so the correct cap value is 2 (not 3) | unlock the built wound/recovery/condition-HUD/First-Aid loop against a SAFE practice dummy WITHOUT pulling in the owner-gated death/respawn system (DIV-0006) | Accepted |
| DIV-0017 | Hostile PvE lethal source | R&E: creatures/NPCs attack for real (natural_attack) at GM discretion | SW_MUSH: wildlife/fauna + hostiles in unsafe zones | `creature_spawn_model` creatures wired (Wave F) as hostile NPCs through the SAME `ground_combat_model` resolution as the sparring arena, dealing UNCAPPED real damage ONLY to players in `effective_security=='lawless'` zones (`LETHAL_SECURITY_TIERS`, owner-tunable to add 'contested'); gives a NON-PvP path to the 'dead' tier so the death loop (DIV-0006) is reachable without the still-gated PvP-consent decision. Starter Mos Eisley zones stay safe | make death reachable in play without touching owner-gated PvP; confine lethality to lawless zones | Accepted; WIRED Wave F S11 (Director-tick `_advance_hostiles` spawns hostiles + engages same-zone players via the S6 lethal flag; default lethal tiers = lawless+contested per the 2026-07-02 ruling) |
| DIV-0018 | Credit economy: prices, markup, sink, loot | R&E: equipment has fixed list credit values (blaster pistol 500, hold-out 275, heavy blaster 750, blast vest 300, vibroblade 250, …); no formal markup/sell/loot economy (GM fiat) | SW_MUSH: credit economy with vendors, buy/sell, loot | WEG-ANCHORED baseline (owner 2026-07-01), wiring in Wave F: each catalog `cost` IS the WEG list price + the BUY anchor (BUY_MARKUP=1.0). Buy = list × director_mult (0.85 trade_boom / 0.90 merchant_arrival, from vendor_model) × (1−bargain, 3%/die cap 50%) × (1−rep_discount, friendly 5%/allied 10%), clamped ≥ 0.35 of list, floor 1. Sell/buy-back = 40% of list (the churn sink). STARTING_CREDITS=1000. Loot on a disabled CREATURE (not the dummy) = scale-tiered credits × pack_size + 25% salvage bundle; v1 credits/salvage only (no item drops) | a modest, WEG-faithful sink that lights up the built vendor/creature/reputation models; SPEND numbers are tunable consts | Accepted; WIRED Wave F S7-S13 (submit_vendor_list/submit_buy/submit_sell RPCs; loot on creature disable via `roll_loot`; rep discount from `org.faction_rep`; two-process verified: 1000cr buy/sell + creature loot) |
| DIV-0019 | Zone-based player-vs-player combat | R&E: combat is symmetric + lethal at GM discretion — any character may attack any other with the SAME attack/damage/soak/wound resolution; no safe-zone/consent concept; initiative = Perception | SW_MUSH: real/lethal combat in unsafe/lawless zones, safe zones protected; full-loot decaying corpse in unsafe zones, credits kept | Owner ruling 2026-07-02: PvP is ZONE-BASED, NO consent handshake. A fire intent may name a player via `intent.target_peer` (0 = the shared dummy/creature, unchanged). `pvp_rules_model.can_fire` gates it from SERVER state (`_peer_zones` + `zones.effective_security`): open PvP ONLY where `effective_security=='lawless'` (`OPEN_PVP_TIERS`) AND shooter+target share a zone — `secured` AND `contested` REJECT player-vs-player fire (PROTECTED), cross-zone shots rejected. DISTINCT from creature lethality (DIV-0017 = lawless+contested); PvP-open is lawless ONLY. The server validates at SUBMIT and re-validates at RESOLUTION (`_build_pvp_gate` -> `resolve_window(seed, pvp_gate)`) to close a Director mid-window tier-flip. Resolution REUSES `ground_combat_model.resolve_exchange_with_action_window` unchanged: the target player's `_players[target]['pools']` are remapped into the `target_*` keys (`defender_target_pools`, `target_stun_mode=false` = REAL damage) + their live `player_wound_severity` projected into the target_state (`defender_target_state`); the result lands on the TARGET PLAYER's state (`maxi`, accumulate) — NOT `_target_state` — so the DIV-0016 sparring clamp is SKIPPED (PvP is lethal). Auto return-fire is suppressed when the defender ALSO queued an intent (`suppress_return_fire = _intents.has(target_peer)`) so a declared duel is one attack each (no double-count) while a passive victim still reaction-fires once; a shooter dropped to sev>=DISABLED before acting loses the queued shot (disabled guard hoisted to the top of the initiative loop, also WEG-faithful); the sparring clamp is floor-aware (`mini(new, maxi(SPARRING_MAX, prior))`) so it can never HEAL a PvP-wounded player who also shot the dummy; `clear_intents_targeting` cancels shots at a player who leaves the zone/disconnects. `resolve_window` returns tiered `casualties` (sev 3-4 = 'out'/First Aid DIV-0013; sev 5 = dead): the net layer routes sev 5 through the SAME `death_penalty_model.apply_death` as DIV-0006 (`_handle_player_death` with a killer_peer, credits kept, durability loss, unequipped-drop corpse, respawn at `WorldState.SPAWN_POINT`) + credits the killer CP/faction/territory influence; in lawless the corpse is FULL-LOOT (`is_full_loot`). Positional inter-player range (nominal `PVP_DISTANCE=12`), third-party corpse-loot RPC + decay, and consensual duels in protected zones are follow-ups | land the owner-decided zone-based open PvP by REUSING the shipped action-window combat + death loop rather than a parallel resolver; gate consent on the already-derived `effective_security` tier; keep hostile-PvE lethality (DIV-0017) + PvP-open distinct per owner | Accepted; WIRED Wave F (shares `_handle_player_death` / DIV-0006 full-loot corpse) |
| DIV-0020 | Quests / missions | R&E: adventures are GM-authored scenes; no formal quest/objective/reward RULE | SW_MUSH: a mission/job system with objectives + rewards | A pure `quest_model.gd` (overnight C2): a character accepts quests from `data/quests_clone_wars.json`; deterministic in-play EVENTS (combat `disable` [optionally target a creature_key], `reach_zone` travel, `earn_credits` gains) advance per-quest progress; at the objective count the quest completes and a ONE-TIME reward (credits + gameplay CP, reusing the existing economy + DIV-0007 tracks) is claimable. No RNG; server owns the live state (sheet.quests). Live wiring (a notice-board RPC + feeding the events) is a follow-up HOT slice | give the world objective-driven "things to do" as a MUD/MMO translation (like DIV-0001 rooms->zones); rewards reuse shipped systems so no new balance fork | Accepted; pure model (wiring is a follow-up) |
| DIV-0022 | PvP consent extension — opt-in duels + bounty-as-consent (layered on the DIV-0019 zone gate) | R&E: PvP is GM-adjudicated, combat lethality at GM discretion; no formal consent/bounty rules (dice mechanics are era- and mode-agnostic) | SW_MUSH: `security_zones_design_v1` three-tier consensual-PvP gradient; `Guide_10` bounties + Bounty Hunters' Guild; `Guide_11` siege forced-PvP window | A pure `pvp_consent_model.gd` (docs/PVP_CONSENT_DESIGN.md) that COMPOSES ON TOP of the shipped DIV-0019 zone gate rather than replacing it: `resolve(attacker, target, ctx) -> {allowed, reason, lethal}` applies the §1.2 precedence — siege hook (off by default) > active mutual DUEL > NEWBIE protection > bounty > zone-open > protected. The zone-open answer (rule 8) is DELEGATED to `pvp_rules_model.can_fire` (lawless-only), so open PvP still fires in lawless regardless of consent and consent only ADDS permission in protected zones — it never removes the zone protection. Layer 2 DUELS (in-memory, non-persisted): challenge->accept->active->ended lifecycle (decline/expire/yield/ko/abort), one-active-duel-per-player invariant, pairwise `duel_active`/`duel_lethal`; an accepted duel makes exactly the A<->B pair mutually attackable in ANY zone (even secured), DEFAULT NON-LETHAL (arena maps `reason=="duel" && !lethal` to a KO clamp at incapacitated sev 3 — a NEW clamp distinct from the DIV-0016 training cap of 2 — auto-stabilize, NO DIV-0006 penalty; lethal-duel opt-in reuses DIV-0006). Layer 3 BOUNTIES (persisted world contract, credit escrow via DIV-0018): one accumulating record per target with `place_bounty`/`bounty_eligible`/`collect_bounty`/`pay_off_bounty`/`expire_bounties`; grants LETHAL attackability to an eligible non-placer hunter in contested+lawless ONLY (secured stays sanctuary), collected on the target's death via the DIV-0006 path; MIN_BOUNTY 250 + 10% posting-fee sink + BOUNTY_MAX 25000 cap + no-self/no-placer-collect + place-cooldown anti-abuse guards. Symmetric one-way NEWBIE protection shields new players from bounty + lawless PvP but NOT from an accepted duel (rule 5 above rule 6). PURE + DETERMINISTIC (NO RNG, no nodes/sockets), non-mutating transitions. HOT wiring (fire-intent consent gate, player-vs-player target + duel KO clamp in combat_arena, duel/bounty RPCs, snapshot fields, death-collection hook) is a follow-up slice | owner direction 2026-07-02: zone-based PvP with an explicit consent stack — safe onboarding + consensual danger (duels/bounties) over open ganking; compose the additive layer on the shipped DIV-0019 gate rather than fork it. NOTE: this row uses DIV-0022 (not DIV-0021, reserved for the sibling siege system) per task assignment to avoid a merge collision | Accepted; pure model (wiring is a follow-up) |
| DIV-0021 | Territory siege / hostile takeover | R&E has no org-vs-org territory-capture rule (GM-adjudicated large-scale conflict) | SW_MUSH Guide_11 §7 specced a "contest state machine, 7-day timer, rival-org no-consent PvP, hostile takeover" but never delivered it ("Planned" — the "Drop 6D") | A pure `siege_state_model.gd` (docs/SIEGE_DESIGN.md §12.1): a deliberate-tempo multi-phase machine (declared->mustering->assault(1..N)->resolution->cooldown, terminal captured/repelled/aborted) on a server-owned control meter [0-100], HIGH capture threshold (75) either held for `capture_hold_seconds` during an assault (early capture) or read at the final tally; the meter accrues only from RESOLVED scoring events (pvp_kill/guard_defeated/control_hold_tick/objective/sabotage) DURING assault windows and BLEEDS toward the defender (0) elsewhere (a lead is perishable). Declare gated by rank 4 + Dominant (40) zone influence + a 10k war-chest escrow; per-node no-active-siege + cooldown, per-org cap of 1 outgoing siege; capped mustering-only third-party intervention (1/side); config is SNAPSHOTTED at declaration (immune to retune) with a compressed prototype/dev timer profile; server owns all timers/scoring; NO RNG in resolution; pvp_consent is DERIVED (true only while state==assault) and NEVER trusted from disk; persists in world_state.dat and resumes via a deterministic restart CATCH-UP (advance through elapsed boundaries + re-apply non-assault bleed). Cumulative unclamped audit points + a replayable contributions[] log run alongside the clamped victory meter. This slice is the PURE model + smoke only; the HOT netcode wiring (declare/join/status RPCs + Director-tick hook + effective-security gate) is a later slice | realize the MUSH's planned-but-undelivered Drop 6D as the headline org-vs-org PvP loop, at the owner's deliberate tempo; buildable + testable now on PvE + presence scoring, with the `pvp_kill` scoring source lighting up when PvP-consent (DIV-0019) is live | Accepted; pure model + smoke (HOT wiring is a follow-up) |
