# Nightly Handoff

## Current Direction

Build a standalone Godot prototype for a Clone Wars-era MMO grounded in WEG Star Wars D6. SW_MUSH is read-only reference material and content inspiration, not a one-to-one port target.

## 2026-06-24 — Direction Update: Multiplayer Foundation (M1)

New owner direction takes the prototype from a deep single-player slice toward an actual MMO: (1) **multiplayer foundation first**, (2) **WEG action-window combat** (server owns seeds/dice), (3) **player-driven & persistent world**, (4) **private/fan IP**. Canonical reference: `docs/MULTIPLAYER_FOUNDATION.md`.

**M1.1 (server-authoritative net core) is DONE and verified.** New: `scripts/net/{world_state,network_manager,net_world}.gd`, `scenes/net_world.tscn`, `scripts/tests/net_smoke.gd`, and the `Net` autoload. Server-authoritative explicit-RPC model — clients send input intents, server owns `WorldState` truth and broadcasts snapshots. Full `check_project.ps1` suite green; two-process headless ENet handshake proven; solo `main.tscn` unaffected.

**M1.2 (shared world + replicated avatars) is DONE (logic).** `main.gd`'s geometry was extracted into `scripts/world/world_builder.gd`; both `main.tscn` (solo) and `net_world.tscn` build the same Mos Eisley from it (no duplication; `world_builder_smoke` covers it). The net client renders a capsule+nameplate avatar per player from snapshots, hides its own (first person), and interpolates remotes. Full gate green (now 30 GDScript smokes + 7 python); handshake re-proven with the real settlement. REMAINING: a human visual check of two GUI clients in Bay 94.

**M1.3 combat CORE is DONE (logic).** `scripts/net/combat_arena.gd` (pure, server-owned) resolves queued fire intents in WEG initiative order each window via `action_window_model` (initiative/declarations) + `ground_combat_model` (attack/damage/return-fire) + `D6Rules`, under a **server-owned seed**, emitting one `combat.exchange.resolved` envelope per shooter — deterministic/replayable, covered by `combat_arena_smoke`. **M1.3b wiring is DONE.** `NetworkManager` holds a server `CombatArena` (loads `prototype_combatants.json`, registers players on join), a `submit_fire_intent` RPC, a ~5s combat-window timer → `resolve_window(server_seed)`, and an `apply_combat_envelope` broadcast; `net_world` client fires on LMB / aims on RMB, shows a HUD combat log, and has `--autofire`/`--combat-window` test affordances. Verified end-to-end via a two-process autofire run: client intent → server WEG resolution (own seed) → envelope → client playback, with target disable+respawn. GDScript smokes + runtime launch + python all green.

**Concurrency + tooling note (2026-06-24):** the repo is now git-tracked (baseline `54fa6b8`) and an **unattended self-paced loop** is set up (`docs/UNATTENDED_BACKLOG.md`, `docs/UNATTENDED_LOOP.md`). A **parallel Codex session is concurrently building the art/asset pipeline** (`tools/fetch_assets.py`, `tools/asset_sources.json`, `MMO_Assets/`, `assets/3d/`, `docs/ASSET_CATALOG.md`). Git use is scoped (commit only changed files, never blanket revert) so the loop never clobbers Codex's in-flight work. **A0 (asset pipeline adopted, gate restored) is DONE.** Root cause of the broken `--import`: Kenney "GLB format" GLBs reference an external `Textures/colormap.png` that the `curate` step dropped (it copied only `.glb`). Fixed `_curated_members` in `tools/fetch_assets.py` to also extract GLB-format textures (dropping FBX/OBJ duplicates), re-curated all 11 Kenney packs, reimported clean. `MMO_Assets/` (925M raw zips) is now gitignored (re-fetchable); the curated `assets/3d/` (41M, 1051 GLBs) is tracked. **Full `check_project.ps1` is GREEN again** (import + 32 smokes + python).

**A1 (increment 1) is DONE.** `scripts/world/world_builder.gd` gained `instance_model`/`place_model` (cached PackedScene) helpers; the Mos Eisley settlement now parks real Kenney low-poly **craft on Bays 86/87 + by the speeder shop** (Bay 94 left clear for the range), uses **factory-kit crate models** for the crate stacks (box collision preserved), and scatters **survival-kit barrels**. All additive/visual-only — existing collision + layout untouched — and shared by both the solo (`main.tscn`) and networked worlds. Model scales/orientations are first-pass: **an owner visual check is worthwhile.** Building-model swaps + scale tuning are queued as **A1b** (after M2).

**M1.4 (persistence backbone) is DONE.** New pure `scripts/net/persistence_store.gd` saves/loads one JSON record per character (shape per `data/schemas/player_persistence.schema.json`; only fields we have now are populated — position + CP/FP/wound + name) under `user://persistence`, covered by `persistence_smoke`. `NetworkManager` gained a `register_account(account_id)` RPC that loads/restores the character on login (server-authoritative position via `WorldState.restore_player`, combat via `CombatArena.set_player_combat`), saves on disconnect, and autosaves every 30s; the client passes `--account <id>` (+ a `--autowalk` headless test affordance). **Verified end-to-end**: a client autowalked from spawn to the world edge (z=-58), disconnected, and a reconnect with the same account was restored to (-20, 1.2, -58). Full `check_project.ps1` green (now 33 GDScript smokes + python). **M1.5 (player identity/nameplate) is DONE.** Clients pass `--name <name>`; it flows via `register_account(account_id, display_name)` to the WorldState player name (snapshot nameplates), the CombatArena player name (combat envelopes show the chosen name, not `Spacer-N`), and the persisted record. Covered by net_smoke (restore_player rename) + combat_arena_smoke (named shooter); verified over the wire (`--name "Mara Jade"` → `Mara Jade hit B1 ...`). Full gate green (33 GDScript smokes + python). **M2.0 (zone & security-state scaffold) is DONE — the persistent world sim begins.** New pure `scripts/net/zone_state.gd` tracks per-zone faction influence (republic/cis/hutt/independent, non-zero-sum) with a DERIVED alert level and DERIVED effective security tier (Hutt≥80 downgrades a tier; a republic_crackdown event upgrades contested→secured), advanced by a **slow deterministic 30s Director tick** (decay toward baseline) that is wholly separate from the 20 Hz movement tick. **No LLM** — the optional Director "flavor" layer is an open owner decision, left off. The server seeds a Mos Eisley spaceport zone and folds its posture into the broadcast snapshot; the client renders an alert/security badge. Covered by `zone_state_smoke` + a two-process check (`zone=high_alert/secured`). Full gate green (34 GDScript smokes + python). **M2.1 (territory-claim scaffold) is DONE.** New pure `scripts/net/territory_model.gd`: an org claims a node in a contested/lawless zone (precondition: zone influence ≥ a foothold floor; secured zones not claimable; one claim per node), deriving an influence tier (foothold/dominant/control) and member effective-security (a lawless node upgrades to contested for owning members). Claims accrue passive income to org treasuries on a 60s resource tick, scaled by tier × risk (lawless yields more, rarer resources). Covered by `territory_smoke`. `NetworkManager` holds the registry + resource tick (a no-op until the org/claim command layer exists). **The siege / Drop-6D hostile-takeover loop is deliberately NOT built — its tuning is an open owner decision.** Full gate green (35 GDScript smokes + python).

**Loop reprioritization:** the remaining queued items split into (a) **content drop 2** (vehicles/starships + droids data — fully gate-verifiable, good for unattended) and (b) **visual items A1b/P1** (building-model scale-fitting, client polish) which produce results only a human can judge. The loop will do content drop 2 next and **defer A1b/P1 until an owner visual check** of the current low-poly world. After content drop 2, the highest-value remaining features (the org/claim command layer + guard NPCs, the siege loop, Force/Jedi, the death-penalty implementation) increasingly need owner design calls — so the loop will trend toward its "needs owner" stop condition.

**Wave C (Character Creation & Progression) is in progress — C1 DONE.** New pure `scripts/rules/chargen_model.gd` implements WEG R&E chargen: `validate_build` enforces exactly 18D of attribute dice within the species min/max ranges plus a 7D skill-dice budget, and produces a starting `sheet` (CP 5 / FP 1 / `force_sensitive` false / `healthy`) in the persistence shape; `default_build` gives a deterministic in-range quick-start. Covered by `chargen_smoke`. Full gate green (36 GDScript smokes + python). **C2 (server chargen flow) is DONE:** `register_account(account_id, name, build)` runs chargen for NEW characters — `_create_character` validates the requested WEG build (or a deterministic quick-start) against the server-loaded species data and persists the sheet; the client passes `--species`/`--quickstart`. Verified over the wire (a new `--species rodian --quickstart` account created a species-aware `dex=3D+1, cp=5` and persisted). Existing characters load unchanged. (Combat still uses the shared trainee pools — wiring the per-character sheet into combat is a later slice.) **C3 (progression model) is DONE:** pure `scripts/rules/progression_model.gd` (WEG R&E / Guide_09) — `skill_raise_cost` = the total-pool dice count (attribute + skill bonus), stepping up at die boundaries, with an optional guild discount; and the DIV-0007 DUAL-TRACK CP wallet `{gameplay_cp, rp_cp}` with `raise_skill` (adds a pip to the skill bonus, spends gameplay-first then RP-prestige, rejects when short) + `award`. Covered by `progression_smoke`. Full gate green (37 GDScript smokes + python). **C4 is DONE — WAVE C (Character Creation & Progression) is COMPLETE.** Disabling the shared combat target awards gameplay CP to the shooter's persisted `sheet.cp_wallet` (`_award_cp`, `COMBAT_CP_REWARD=3`, tunable); a `submit_skill_raise(skill)` RPC validates the spend via `progression_model` against the character's wallet, governing attribute (looked up from the skill catalog), and current bonus, applies the new bonus to the sheet, persists, and replies; the server pushes the wallet to the client via `apply_wallet`. The client renders a CP HUD and raises Blaster with `K` (or `--raise-skill` headless). Verified over the wire: a new quickstart character earned CP from kills and raised `blaster 0D -> 0D+1 (cost 3)`, persisted. Full gate green (37 GDScript smokes + python).

**Wave D (combat uses the character sheet) followed Wave C; D1 is DONE.** `scripts/net/combat_arena.gd` now builds each player's combat pools from their persisted sheet (attack = DEX + blaster bonus, dodge = DEX + dodge bonus, soak = STR; the target side stays shared; damage uses a default starter blaster until an inventory exists). `register_player`/`set_player_sheet` take a sheet (no sheet = trainee fallback, backward-compatible); `NetworkManager` applies it on login and re-applies on each skill raise (so a raise strengthens your attack immediately). Verified over the wire: a new quickstart character fought with attack pool DEX 3D, and raising Blaster grew it to 3D+1. Full gate green (37 GDScript smokes + python).

**Unattended driver change (2026-06-25):** the loop is now driven by a recurring **CronCreate** timer (~every 10 min, fires only when idle so no overlap; session-bound, 7-day auto-expire) instead of manual `/loop` — and runs WITHOUT owner questions (engineer-level decisions; owner-gated forks parked as BLOCKED-needs-owner). **D2 (combat uses equipped weapon + armor) is DONE.** Chargen sheets now carry a starter `equipment {weapon: blaster_pistol, armor: blast_vest}`; `combat_arena` takes weapon/armor catalogs and builds each player's `damage_pool` from the equipped weapon and `player_armor` from the equipped armor (fallbacks when absent); `NetworkManager` loads `weapons_clone_wars.json` + `armor_clone_wars.json` and passes them in. `chargen_smoke`/`combat_arena_smoke` extended (equipped heavy blaster → 5D damage; no equipment → default). Full gate green (37 GDScript smokes + python). Combat now reflects the full character: attributes, skills, AND gear. Known follow-up (D3): an inventory/equipment-swap system so players can change loadout. NOTE: a parallel session is concurrently working the asset pipeline (`docs/ASSET_PIPELINE.md`, `assets/`); my Wave C work is in `scripts/` and stays scoped to avoid collisions. **M2.2 (Director world events) is DONE (self-extended world-sim).** `scripts/net/zone_state.gd` now fires one deterministic world event at a time per zone — when no event is active it rolls `hash(tick:zone) % 100` against `EVENT_CHANCE` and, on a hit, picks a type from a **fixed 12-event menu** keyed off the zone's dominant influence (republic crackdown/checkpoint, hutt bounty/auction, cis propaganda/pirate, else a neutral Tatooine list: merchant caravan, sandstorm, cantina brawl, distress signal, krayt sighting, trade boom). Each event carries a Clone Wars headline, lasts `EVENT_DURATION` slow ticks, and surfaces in `zone_summary` as `event`/`event_type`; the client renders a **NEWS** HUD line and logs `[news] <headline>` on change. **Still no LLM** — the optional Director "flavor" layer stays the open owner decision; this is the deterministic baseline. A `--director-tick <secs>` server override drives the slow tick fast for headless verification. Covered by an extended `zone_state_smoke` (an event fires within 40 ticks, is from the menu, and is deterministic); verified over the wire (a client received neutral Tatooine headlines streaming from the server). Full gate green (37 GDScript smokes + python).

**Clean-session handoff + Wave E stocked (2026-06-25).** Ahead of the owner being away for an all-day unattended push, ran a 5-reader **Workflow** audit (rules / netcode / world-sim+data / tests / docs, ~560k tokens) over the whole prototype and synthesized a deep, breadth-first backlog — `docs/UNATTENDED_BACKLOG.md` → **Wave E (E1–E27)** — explicitly tagged `[PAR]` (parallel-safe pure-model+test/docs slices) vs `[HOT]` (serialize on `network_manager.gd`/`net_world.gd`) so the loop can batch parallel work via Workflow and serialize hot-file work. Wrote `docs/SESSION_HANDOFF.md` as the canonical clean-session entry point (current state + the re-armable all-day loop contract + the parallelization playbook + guardrails + the owner-gated park list); added a "read SESSION_HANDOFF first" pointer to `CLAUDE.md` and refreshed its stale milestone section. **Key mechanism note:** the CronCreate driver is *session-only* and does not survive a session switch, so the handoff's first action is for the new session to **re-arm its own cron** (≈10-min, idle-only) from the embedded contract. Retired the prior session's stale cron to avoid a double-driver. Highest-value Wave E threads: restore the WEG cumulative wound ladder + Wounded-Twice tier (E2, ledger-first), wound recovery/healing (E3), the player→world influence loop (E8→E24), the org claim command layer (E9→E23), and finally consuming the orphaned creatures/droids data (E10/E11). Docs-only + backlog changes — gate unaffected and green.

**Wave E loop resumed (2026-06-25).** A fresh clean session re-armed the ~10-min idle-only CronCreate driver from `docs/SESSION_HANDOFF.md` (the prior session's cron died with it) and confirmed the full gate green at HEAD `5ba15f0` before touching anything. **E1 (reconcile stale roadmap docs) is DONE** — docs-only: advanced `MULTIPLAYER_FOUNDATION.md` Status/Roadmap (M1.3 DONE; M1.3b/M1.4/M1.5/M2.0–2.2/Wave C/Wave D added; Wave E in-progress), deleted the self-contradicted "loop has STOPPED at Wave C" sentence here, archived `NEXT_DECISIONS.md`, and corrected the stale "`--import` can fail" caveat in `UNATTENDED_LOOP.md` + the backlog Guardrails (A0's colormap fix is live; the full `check_project.ps1` is the bar). No code touched.

**(Earlier) Content drop 2 was DONE and the loop stopped at its designed handoff point before the owner chose Wave C.** New (one-way from read-only SW_MUSH): `data/starships_clone_wars.json` (6 era-appropriate civilian craft — GCW/Imperial ships excluded, `content_smoke` asserts none leak in), `data/droids_clone_wars.json` (3 commerce droids), `data/creatures_clone_wars.json` (22 wildlife). The backlog is now dry of unblocked, non-owner-decision, non-visual work. **What remains needs the owner:** a visual check of the low-poly world (then A1b building models + P1 polish), and design decisions on the next headline features — the org/claim command layer + guard NPCs, the **siege/Drop-6D loop** (durations/threshold), **Force/Jedi access & scarcity**, the **death-penalty implementation** (DIV-0006 shape), and whether the **LLM Director** ships at launch. Full gate green throughout (35 GDScript smokes + 7 python).

**Parallel streams integrated (2026-06-24):** content drop 1 — `data/{weg_skill_catalog,species_clone_wars,weapons_clone_wars,armor_clone_wars}.json` (76 skills / 9 species / 32 weapons / 23 armors, ported one-way from read-only SW_MUSH, covered by `content_smoke`); and the persistent world-sim design — `docs/{WORLD_SIM_DESIGN,FACTION_TERRITORY_DESIGN,PERSISTENCE_DESIGN}.md` + `data/schemas/*.json` (Director AI, security zones, the Drop 6D siege state machine, SQLite→Postgres persistence). Gate now 32 GDScript smokes + 7 python; all green.

## Current Playable Slice

- 3D Mos Eisley Spaceport Row / Bay 94 range.
- First-person movement, location and range-target inspection with current/next live behavior explanation, aiming, cover, dodge, full dodge, live staggered remote-pressure volleys, coordinated remote fire-team holds, peeking/tucked covered remotes, near-miss remote pinning, flanking/repositioning remote holds, reload/weapon-cycle remote holds, covering-fire holds, morale-hesitation remote holds, wounded-remote fallback timers, hit suppression, CP, FP, torso-only vest armor soak with visible coverage/quality pips, in-world live remote-state badges, in-world hit-location feedback, persistent body-part damage tinting and scorch/impact marks, stun damage, scale targets, a sine-sweeping B1 range target, a patrol-pattern walker-scale armor target, seeded combat logs with action-window declaration summaries, and live range telemetry. Passive HUD/overlay controls now ignore mouse input, player look/gameplay hotkeys are handled through `_input`, captured-mouse blaster/inspect rays aim from the center reticle, normal dodge moved from `D` to `Q` so WASD movement no longer conflicts with combat declarations, and `Z` pauses/resumes the live remote pressure clock.
- 2.5D Mos Eisley approach mode with opaque modal presentation, shared/tested modal-state detection for CanvasLayer and CanvasItem screens, paused ground controls/range pressure/HUD/character-sheet overlay/target motion, cursor/input capture, a tested responsive viewport-sized tactical plane that rebuilds on viewport changes, model-formatted bridge-mode status with current maneuver difficulty/named-hazard preview, state-aware bridge action buttons with compact maneuver difficulty/hazard-count cues that refresh after piloting maneuvers, model-formatted bridge crew/station strip with commander station, banked-assist requested-alias and station-round visibility, and crew-wound visibility, D6 crew-wound penalties on station actions including ship-aware sensor sweeps/contact identification with wound callouts in action readouts, clickable/selectable contacts, keyboard target cycling, logged target selection, clickable multi-hazard approach zones with collision and non-collision hazard detail/log readouts plus current-maneuver crossing/avoidance preview, clickable station/action controls, selected-contact detail readout with range-aware lock state, WEG scale posture, defensive posture, hull/shield soak posture, weapon posture, crew wound posture, systems posture, model-formatted bridge cue with action hotkeys, movement posture, hostile-fire readiness, counterfire posture, destroyed-weapon-aware and custom data-preserving field-repair options/routing with visible data-driven difficulty labels and yard-only-aware automatic targeting demonstrated on visible traffic, and targeting-penalty preview, capped recent-action log, deterministic sensor sweeps, persistent revealed contacts, per-sweep new-contact callouts, model-formatted contact labels, sensor, contact-identification, comms hail, traffic tick, manual traffic stepping, gunnery action, confidence-modified hidden-target gunnery, gunnery targeting, lock-disruption, counterfire, automatic hostile-fire, ship-condition, shield-reroute, station-assist action, damage-control action with WEG difficulty-name and yard-only callouts, astrogation plot action, maneuver action, assist-replacement, maneuver-hazard, maneuver-collision, crew-wound, and break-lock readouts that show tracks, consumed sensor assists, current known/hidden contact totals, movement, range holds, authoritative current weapon-solution pressure, data-driven successful-hail lock delays and failed-hail lock pressure with ready-lock callouts, ready hostile-fire opportunities, automatic hostile shots, resulting player ship condition, spent/cleared hostile locks, return-fire results, condition countdown changes, and hazard details, persisted margin-based contact confidence, persisted selected-contact identification profiles and comms dispositions with comms lock-delay/pressure context, confidence-annotated gunnery targeting context, combined track/weapon-solution engagement summaries in action and status readouts, confidence-aware lock fallback telemetry, known/hidden contact telemetry, a live traffic clock that advances server-style WEG accounting ticks without player turn advancement, live tactical telemetry with latest automatic hostile-fire summary, banked crew-assist target and station-round visibility with preserved requested target aliases, consumed assist callouts with requested-alias context across player station actions, replacement-aware station assist banking with requested-alias replacement text, helm/fire-control/shield/sensor/communications/engineering/navigation-family station-assist aliases for evasive/weapon-solution/deflector/identification/targeting/hailing/field-repair/route-plot support, commander tactical-coordination assists routed through sensor targeting, and preserved requested assist targets in station audit text, persistent hull-severity status, system-damage flags, deduplicated field-repair counts that exclude yard-only systems, and crew-wound counts, moving/player-tracking contacts, hostile engagement ranges, live hostile fire from ready weapon-solution clocks, blocked-fire stale-lock cleanup for disabled/destroyed hostiles and destroyed player ships, same-tick prior-fire destruction blocking, consumed-lock telemetry cleanup, weapon-solution-gated counterfire with spent-lock telemetry, evasive/gunnery lock disruption, seeded gunnery, shield soak, shield rerouting, rotating crew-station assists, navigator astrogation plotting, data-driven per-system repair difficulty overrides, player-prioritized damage control with formatted before/after condition changes, starship damage/system results, and lightweight ship condition persistence.

## Rules Implemented In Prototype

- Dice pools, pips, Wild Die, Character Point dice, Force Point doubling.
- Ranged difficulty by range band.
- Cover dice and full-cover blocking.
- Aim up to +3D.
- Normal dodge and full dodge.
- Live ground range pressure: active remotes evaluate deterministic incoming-fire windows on a short clock, with target-specific cadence/phase, peeking/tucked, flanking/repositioning, reload/weapon-cycle, covering-fire, morale-hesitation, and fallback metadata staggering which remotes fire on each automatic tick; `V` remains an immediate debug/manual volley from every eligible armed remote.
- Range target return-fire eligibility separates shootable targets from armed combatants, so inert scale/armor drills can be shot without adding bogus remote fire.
- Range target live-fire cadence keeps automatic encounter pressure paced without changing the WEG D6 attack, defense, damage, or soak resolution path.
- Range target suppression metadata lets successful player hits push active remotes off upcoming live-fire ticks without changing WEG attack or damage resolution.
- Range target pinning metadata lets close player misses briefly push configured remotes off upcoming live-fire ticks through a smoke-tested model helper, adding suppressive pressure behavior without changing WEG hit, damage, or soak results.
- Range target peeking metadata lets covered remotes alternate exposed and tucked live ticks, delaying shots as encounter behavior without changing WEG attack, cover, damage, or soak rules.
- Range target flanking metadata lets selected remotes spend configured live ticks repositioning instead of firing, adding NPC pressure variation without changing WEG attack, cover, damage, or soak rules.
- Range target reload metadata lets selected remotes spend configured live ticks cycling weapons instead of firing, adding automatic-fire pacing without changing WEG attack, damage, or soak rules.
- Range target covering-fire metadata lets selected remotes spend configured live ticks applying non-damaging pressure instead of resolving a shot, making live pressure less metronomic without changing WEG attack, damage, or soak rules.
- Range target morale metadata lets selected wounded/stunned remotes spend configured live ticks hesitating instead of firing, adding NPC behavior texture without changing WEG wound penalties, attack, damage, or soak rules.
- Range target coordination metadata lets a ready fire-team pick one shooter per live tick while lower-priority wingmates hold in a `coordinating` state, pacing NPC pressure without changing WEG attack, cover, damage, or soak rules.
- Range target fallback metadata lets remotes that cross a configured wound threshold briefly stop contributing live return fire, adding wounded-NPC behavior without changing WEG wound penalties or attack/damage/soak resolution.
- Ground range telemetry formats the live pressure countdown plus model-derived armed, next-scheduled, suppressed, pinned, currently covered, fallback, coordinating, flanking, reloading, hesitating, and covering return-fire source counts, cover, defense, CP/FP queues, wound state, automatic-volley count, and latest combat audit summary for the HUD, including compact ready/next/suppressed/pinned/covered/fallback/coordinating/flanking/reloading/hesitating/covering pressure hints plus hit-location/armor coverage hints extracted from the latest envelope.
- `range_target_model.gd` now summarizes mixed live remote participant snapshots into ready, waiting, suppressed, pinned, fallback, hesitating, covered, coordinating, flanking, reloading, covering, disabled, inert, and armed counts, keeping Bay 94 telemetry aligned with future server-authoritative encounter accounting.
- `range_state_badge_model.gd` formats the in-world labels over Bay 94 targets and the inspection explanations for those labels, so the current model-derived behavior state is visible without reading HUD telemetry and understandable when inspected.
- Moving range targets use a reusable live motion controller and pure motion model with target-specific sine and patrol/triangle patterns; the B1 remote and walker-scale armor target turn WEG attack/scale resolution into real-time aim/tracking tests, and disabled targets stop moving once WEG damage pushes them to incapacitated severity.
- Range target inspection uses a smoke-tested pure formatter to show profile name, scale, cover, wound state, soak pool, armor coverage/current quality, armed or inert status, weapon pool, and source note when the player presses `E` while looking at a target.
- Range hit feedback uses a smoke-tested pure formatter plus a short-lived in-world marker/label parented to the target, showing hit/miss/blocked state, WEG hit location, armor coverage/degradation, and wound result from the same exchange packet used by telemetry. Successful hits are also recorded on the target and reapplied as persistent body-part material tinting plus small scorch/impact marks until the range drill resets.
- Cached defense rolls for future multi-attacker exchanges.
- Multi-action penalties.
- Wound/stun penalties.
- Armor protection by energy/physical type and Dexterity penalties.
- Armor condition now supports deterministic hit locations, explicit torso-only training blast vest coverage, optional partial-coverage armor soak, and persistent quality-pip degradation when damaging hits penetrate protected armor, feeding later soak rolls without replacing WEG wound or damage bands.
- WEG scale helpers for attack, dodge, damage, and soak.
- Space shields add to hull soak for covered non-ion gunnery hits; ion-tagged weapons bypass shields.
- Starship-shields station action can reroute listed shield dice across front/rear/left/right arcs, with the full reroute action readout formatted and smoke-tested in the status model.
- Crew-station assist actions are data-driven and rotate through pilot, commander, sensors, communications, gunnery, shields, and repair support; each can bank a one-shot bonus for the next matching action, helm/piloting targets such as `helm`, `helm_control`, `evasive`, `break_lock`, `course_correction`, and `approach_vector` normalize to `maneuver`, fire-control targets such as `fire_control`, `target_lock`, `weapon_lock`, `weapon_solution`, `bracketing`, `turret`, and `cannon` normalize to `gunnery`, shield-operation targets such as `shield_reroute`, `starship_shields`, `deflectors`, `deflector_shields`, `angle_shields`, and `shield_timing` normalize to `shields`, sensor-family targets such as `identify`, `identification`, `id`, `targeting`, `targeting_solution`, and `sensor_lock` normalize to `sensors`, communications-family targets such as `hail_contact`, `contact_hail`, `traffic_control`, `transmit`, `broadcast`, and `transponder_ping` normalize to `communications`, engineering-family targets such as `engineering`, `damage_control_repair`, `field_repair`, `patching`, and `systems_repair` normalize to `repair`, navigation-family targets such as `nav_plot`, `route_plot`, `jump_plot`, `jump_calculation`, `hyperdrive_calculation`, `hyperspace_route`, and `local_jump` normalize to `astrogation`, requested target names are preserved in event/banked/consumed-assist packets for audit text when they differ from the normalized target, full station-assist action readouts are smoke-tested status-model text, the live action readout names the consumed assist and requested alias when it is spent, and replacement events record which pending assist was superseded using the same formatter while preserving old/new requested aliases.
- Persisted crew wounds now apply WEG-style station action penalties: stunned/wounded crew reduce matching station pools by 1D, incapacitated or worse crew zero the matching station pool, and the event packets plus smoke-tested status readouts record the station wound context for sensor sweep, contact identification, maneuver, gunnery/defense, shield reroute, damage control, astrogation, communications, and crew-station assist actions.
- Navigator astrogation plot actions roll astrogation/nav pools plus consumed navigator assists against current nav penalties, clear hyperdrive calculation and astrogation difficulty penalties on success, fail cleanly when the hyperdrive is disabled, and use a smoke-tested status-model readout.
- Space sensor sweeps now classify roll margins into missed/faint/partial/solid/clear contact confidence, expose the classification on each sensor event, persist each contact's best-known confidence in tactical state, preserve already revealed contacts after later weak sweeps, report newly revealed contacts per sweep, call out successful tracks, new contacts, consumed assists, and current known/hidden totals through a test-covered status-model readout, show successful track quality in smoke-tested status-model contact labels, annotate gunnery events/readouts with the target's current track confidence through test-covered status-model targeting text, and add WEG-style gunnery difficulty modifiers for hidden or sensor-required targets based on unresolved/faint/partial/solid/clear track quality.
- Contact-identification scans use the selected contact's current sensor confidence as a difficulty modifier, consume banked sensor assists, persist a curated transponder/affiliation/threat profile on success, and show the resulting ID on contact labels without changing gunnery hit math yet.
- Communications hails roll the ship's communications pool against selected-contact identity/threat pressure, consume banked communications assists, persist responsive/unresponsive contact disposition, display data-driven hail responses, and can data-drive a successful-hail delay or failed-hail pressure advance on the selected contact's weapon-solution clock without changing gunnery hit math; pressure packets now flag ready locks when the failed hail completes the hostile firing solution, and comms lock context is persisted into the disposition so contact labels/details keep showing the lock change after the immediate hail readout.
- Space contacts can use data-driven heading movement profiles advanced by a deterministic tactical movement tick; destroyed, drives-disabled, and control-locked contacts now emit explicit movement-block reasons in event packets and traffic readouts, and the overlay drives those ticks on a live five-second clock while keeping `T` as pause/resume for the clock.
- Space contacts can optionally track the player ship during live accounting ticks with a capped turn rate; the Sensor Shadow now uses this as a first hostile behavior profile.
- Player-tracking contacts can hold a configured engagement range once close enough; the Sensor Shadow currently holds at Short range instead of closing forever.
- Armed contacts holding engagement range build stateful weapon-solution clocks with WEG range-band text; ready clocks emit hostile-fire opportunity events and the live overlay can now resolve those into automatic WEG gunnery/damage shots. The Sensor Shadow currently becomes ready after two held-solution ticks.
- Contact movement events now include combined engagement context that summarizes persisted sensor confidence and weapon-solution buildup/ready state for UI and future server telemetry without adding a new combat gate.
- Selected-contact detail now formats WEG scale posture from contact scale data, making cross-scale combat context visible in the side panel before gunnery resolves.
- Selected-contact detail now formats defensive posture from the target's WEG defensive maneuver pool and persistent condition state, reporting controls, maneuverability, drives-offline, controls-dead, or destroyed defense changes in the side panel.
- Selected-contact detail now formats hull/shield soak posture from hull pools, shield pools/arcs, shield loss, and controls-ionized state, reporting target resistance inputs in the side panel without rolling damage.
- Selected-contact detail now formats weapon posture from gunnery pool, fire control, weapon damage, controls-ionized, weapons-offline, and destroyed state, reporting target offensive inputs in the side panel without rolling an attack.
- Selected-contact detail now formats crew wound posture from persisted passenger/gunner damage packets, including stunned crew that can still penalize station actions.
- Selected-contact detail now formats systems posture from Move loss, hyperdrive disabled/calculation state, astrogation difficulty penalties, generator overload, structural damage, and destroyed state, reporting non-weapon/non-defense system state in the side panel without rolling repair.
- Selected-contact detail now formats a bridge cue with action hotkeys from sensor, identification, communications, weapon-solution, sensor-targeting, repairable-condition, and destroyed-state data; the bridge header echoes that next-action cue, colors it by smoke-tested urgency level, and the bridge action row highlights/tints the matching cue buttons from the same structured urgency metadata without adding a new mechanics gate.
- The bridge cue now falls back to local player-ship damage control when no selected-contact cue is more urgent and the local ship has field-repairable damage, matching the damage-control action's player-first repair routing.
- Combined bridge-cue priority is smoke-tested in the status model: destroyed player ship overrides all, selected-contact threats/actions outrank routine local repairs, and local field repair fills otherwise-idle bridge guidance.
- Bridge-cue urgency is smoke-tested in the mode model: abandon-ship cues are critical alerts, destroyed selected-target cues render as neutral status, hostile lock/return-fire cues render with threat wording, local damage-control cues render with repair wording, routine prompts render as next-action guidance, highlighted action buttons carry the matching status metadata plus tooltip wording for consistent tinting, and cue-following player actions get matching compact tags in the recent-action log.
- Space overlay layout now reserves smoke-tested wrapped header and traffic slots above the tactical map, keeping longer bridge cues readable without overlapping the 2.5D play area.
- Selected-contact detail now formats movement posture from the latest movement event, reporting tracking, closing, holding range, patrol movement, or movement blocks in the side panel.
- Selected-contact detail now formats hostile-fire readiness from counterfire, weapon-solution clock, and condition state, reporting acquiring, partial, ready-to-fire, armed, destroyed, or weapons-offline posture in the side panel.
- Successful evasive piloting maneuvers can clear hostile weapon-solution clocks; failed maneuvers keep lock pressure in place.
- Successful player gunnery hits against a locked contact disrupt that target's weapon-solution clock and use a smoke-tested status-model readout for the disruption callout; misses keep lock pressure in place.
- Selected hostile contacts can require a ready weapon-solution clock before counterfiring; the Sensor Shadow now uses this stricter pacing and consumes the ready lock when it fires, reporting the spent lock rounds in the event and a smoke-tested status-model overlay readout.
- Live space accounting ticks combine contact movement with player/contact condition countdowns for ionization, pilot penalties, and control locks, then format movement, range holds, movement blocks, weapon-solution pressure, ready hostile-fire opportunities, automatic hostile shots, resulting player ship condition, and changed conditions through a smoke-tested status-model readout.
- Space tactical telemetry formats live traffic countdown, contact/known/hidden counts, hostile weapon-solution pressure with track confidence from movement engagement context or persisted sensor confidence fallback, latest automatic hostile-fire summary, banked crew-station assists with target actions, requested target aliases when they differ, and station-round age, player ship condition using persistent `worst_hull_severity`/destroyed state, system-damage flags, derived repair counts, crew-wound counts, and automatic update count for the overlay. Current `weapon_solution_counts` is authoritative, so consumed or cleared locks no longer linger through stale movement-event context.
- Space map contact dots and labels are selectable in the modal overlay; the selected contact is highlighted with a smoke-tested detail readout for track quality, ID, comms disposition, weapon-solution lock pressure, sensor-targeting difficulty, and condition, then drives gunnery plus target-side damage-control fallback instead of relying only on the data file's default target.
- Space target cycling is smoke-tested through a pure selection model and wired to `Tab`/`.` for next contact plus `,` for previous contact, so the overlay can be driven without precise mouse clicks; click and keyboard selection now use a smoke-tested formatter, update the scan readout, and append to the recent-action log as `Target` actions.
- Space player-triggered actions now append to a capped recent-action log in the overlay through a smoke-tested pure log model; automatic live traffic ticks stay out of the log to avoid spam, manual `;` traffic steps are logged as deliberate bridge actions, and actions taken through currently highlighted bridge cues carry compact Alert/Threat/Repair/Status/Next tags that are consumed after the first log entry so later target/hazard selections do not inherit stale urgency.
- Space gunnery can derive incoming front/rear/left/right shield arcs from target heading and relative attacker position, with scripted overrides still supported.
- Armed hostile contacts can opt into seeded counterfire after the player's gunnery exchange, and ready live-tick hostile fire uses the same WEG gunnery, scale, shield, hull, system-effect, passenger-damage, crew-wound, and condition-persistence path as player fire. Hostile fire does not consume player-banked station assists; blocked/applied counterfire and automatic hostile-fire summaries are formatted by the status model, including the resulting player ship condition. If a disabled/destroyed hostile or destroyed player ship reaches a ready-fire check, the stale weapon-solution lock is cleared and reported as cleared rather than left in pressure state. If prior automatic fire in the same tick destroys the player ship, later ready hostile shots are blocked as prior-fire destruction and clear their locks.
- Piloting maneuver actions roll piloting plus maneuverability, update heading/position, record WEG movement failures, and now use a smoke-tested status-model action readout.
- Piloting maneuver actions can evaluate data-driven circular approach hazards that increase difficulty and mark obstacle-present collision risk when crossed, with hazard readouts now formatted by the smoke-tested status model, including compact names for multiple crossed hazards.
- Space maneuver hazards are rendered on the tactical map as visible approach zones with difficulty labels; hazard zones are clickable and write a status-model detail readout with piloting modifier, radius, collision-risk wording, and current planned route crossing/avoidance into the scan panel/action log.
- Space gunnery events translate damage margins through WEG starship damage bands; ion hits translate to controls-ionized results; full gunnery action readouts are now smoke-tested status-model text.
- Space gunnery rolls WEG system-effect entries for light/heavy/severe damage.
- Space gunnery returns updated target condition for shield loss, controls ionized, hull severity, system effects, lost Move, disabled weapons/drives, hyperdrive flags, structural damage, and destruction flags.
- Later space gunnery exchanges apply shield loss and controls-ionized penalties to shields, fire control, weapon damage, and defensive maneuver pools.
- Later space gunnery exchanges also apply specific system effects: maneuverability loss reduces defensive maneuver pools, disabled drives remove defensive maneuver, and disabled weapons prevent attacks.
- Controls-ionized condition can count down across ship-condition rounds.
- Damage-control repair actions use WEG repair difficulty bands, can read data-driven per-system difficulty overrides from ship or condition dictionaries, report WEG difficulty names beside the target number, explicitly report field repair unavailable for yard-only systems, report free field-repair time and dockyard credit quotes, can clear one targeted damaged system, and now use a smoke-tested status-model action readout.
- Damage-control events carry before/after condition summaries and consumed crew-assist summaries for server audit logs and live repair-change overlays.
- Live damage-control actions prioritize the local player ship's first field-repairable condition, skip destroyed weapons and yard-only systems, then fall back to the drill target if the local ship has no field-repairable damage.
- Ship condition summaries turn persistent damage state into readable hull status, active flags, deduplicated repairable-system lists, field-repair counts that exclude yard-only systems in compact telemetry, and crew-wound counts for overlays; gunnery, repair, maneuver, and counterfire action readouts now share the smoke-tested status-model condition formatter.
- Passenger/gunner damage events use the WEG character-scale passenger damage chart for light, heavy, severe, and destroyed ship results.
- Named ship crew can roll soak against passenger/gunner damage and persist WEG wound results in ship condition.
- Failed piloting maneuvers use WEG movement-failure bands; collision-band failures become wild spins in open space or speed-based hull collisions when obstacles are present, with collision, crew-wound, and broken-lock readouts now smoke-tested in the status model.
- Action-window initiative/declaration/resolution scaffold.
- Live range exchanges now use `range_action_window_model.gd` to summarize player/remote declarations and assemble ready-for-resolution action-window packets; `ground_combat_model.gd` exposes packet-driven resolution entry points that validate those packets before resolving WEG dice; `combat_event_envelope_model.gd` packages resolved results into server-style event envelopes with optional encounter-state snapshots; `combat_event_log_model.gd` keeps a capped local audit history for future headless-server logging.
- Bay 94 attaches `range_pressure` encounter snapshots to shot and incoming-fire envelopes, including current and next live remote-state summaries, so audit logs can explain what pressure existed when a WEG action window resolved.
- `combat_event_log_model.gd` now extracts the latest `range_pressure` ready/next/suppressed/pinned/covered/fallback/coordinating/flanking/reloading/hesitating/covering counts and latest hit-location/armor coverage result into its compact summary, and `range_status_model.gd` shows that in the HUD audit suffix.
- Character-sheet model for WEG attributes, skill bonuses, gear lookup, CP, FP, wounds, and combat-pool extraction.
- Character sheet overlay toggled with `H`, including live range CP/FP/wound/cover/defense state plus armor coverage and current armor-quality pips.

## Validation

Run from the project root:

```powershell
$machinePath = [Environment]::GetEnvironmentVariable('Path', 'Machine')
$userPath = [Environment]::GetEnvironmentVariable('Path', 'User')
$env:Path = @($machinePath, $userPath) -join ';'
.\tools\check_project.ps1
```

Latest validation in this session passed all checks after improving maneuver action readouts so multi-hazard routes name the first crossed hazards in compact status text.

## Next Best Slices

- Add richer moving-target and remote-fire encounter behavior beyond sine/patrol movement, cadence/phase firing, model-derived state summaries, peeking/tucked cycles, flanking holds, reload/weapon-cycle holds, covering-fire holds, morale hesitation, wounded fallback, and hit suppression.
- Expand the space drill into broader crew-station action variety, richer hazard consequences, target behavior, encounter pacing, and deeper confidence-informed targeting.
- Expand armor UI beyond the range HUD/target inspection/hit markers/body-part tinting, such as richer scorch decals or armor plate breakup.
- Add NPC/remote behavior states such as flanking, morale, and coordinated suppression.

## Standing Constraints

- Keep `C:\SW_MUSH` read-only.
- Keep Clone Wars-only era framing.
- WEG R&E leads mechanics; SW_MUSH code/data is a reference when faithful or useful.
- Prefer free/open tooling and cheap local validation.
