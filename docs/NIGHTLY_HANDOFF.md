# Nightly Handoff

## Current Direction

Build a standalone Godot prototype for a Clone Wars-era MMO grounded in WEG Star Wars D6. SW_MUSH is read-only reference material and content inspiration, not a one-to-one port target.

## 2026-06-24 — Direction Update: Multiplayer Foundation (M1)

New owner direction takes the prototype from a deep single-player slice toward an actual MMO: (1) **multiplayer foundation first**, (2) **WEG action-window combat** (server owns seeds/dice), (3) **player-driven & persistent world**, (4) **private/fan IP**. Canonical reference: `docs/MULTIPLAYER_FOUNDATION.md`.

**M1.1 (server-authoritative net core) is DONE and verified.** New: `scripts/net/{world_state,network_manager,net_world}.gd`, `scenes/net_world.tscn`, `scripts/tests/net_smoke.gd`, and the `Net` autoload. Server-authoritative explicit-RPC model — clients send input intents, server owns `WorldState` truth and broadcasts snapshots. Full `check_project.ps1` suite green; two-process headless ENet handshake proven; solo `main.tscn` unaffected.

**M1.2 (shared world + replicated avatars) is DONE (logic).** `main.gd`'s geometry was extracted into `scripts/world/world_builder.gd`; both `main.tscn` (solo) and `net_world.tscn` build the same Mos Eisley from it (no duplication; `world_builder_smoke` covers it). The net client renders a capsule+nameplate avatar per player from snapshots, hides its own (first person), and interpolates remotes. Full gate green (now 30 GDScript smokes + 7 python); handshake re-proven with the real settlement. REMAINING: a human visual check of two GUI clients in Bay 94.

**M1.3 combat CORE is DONE (logic).** `scripts/net/combat_arena.gd` (pure, server-owned) resolves queued fire intents in WEG initiative order each window via `action_window_model` (initiative/declarations) + `ground_combat_model` (attack/damage/return-fire) + `D6Rules`, under a **server-owned seed**, emitting one `combat.exchange.resolved` envelope per shooter — deterministic/replayable, covered by `combat_arena_smoke`. **M1.3b wiring is DONE.** `NetworkManager` holds a server `CombatArena` (loads `prototype_combatants.json`, registers players on join), a `submit_fire_intent` RPC, a ~5s combat-window timer → `resolve_window(server_seed)`, and an `apply_combat_envelope` broadcast; `net_world` client fires on LMB / aims on RMB, shows a HUD combat log, and has `--autofire`/`--combat-window` test affordances. Verified end-to-end via a two-process autofire run: client intent → server WEG resolution (own seed) → envelope → client playback, with target disable+respawn. GDScript smokes + runtime launch + python all green.

**Concurrency + tooling note (2026-06-24):** the repo is now git-tracked (baseline `54fa6b8`) and an **unattended self-paced loop** is set up (`docs/UNATTENDED_BACKLOG.md`, `docs/UNATTENDED_LOOP.md`). A **parallel Codex session is concurrently building the art/asset pipeline** (`tools/fetch_assets.py`, `tools/asset_sources.json`, `MMO_Assets/`, `assets/3d/`, `docs/ASSET_CATALOG.md`). Git use is scoped (commit only changed files, never blanket revert) so the loop never clobbers Codex's in-flight work. **A0 (asset pipeline adopted, gate restored) is DONE.** Root cause of the broken `--import`: Kenney "GLB format" GLBs reference an external `Textures/colormap.png` that the `curate` step dropped (it copied only `.glb`). Fixed `_curated_members` in `tools/fetch_assets.py` to also extract GLB-format textures (dropping FBX/OBJ duplicates), re-curated all 11 Kenney packs, reimported clean. `MMO_Assets/` (925M raw zips) is now gitignored (re-fetchable); the curated `assets/3d/` (41M, 1051 GLBs) is tracked. **Full `check_project.ps1` is GREEN again** (import + 32 smokes + python).

**A1 (increment 1) is DONE.** `scripts/world/world_builder.gd` gained `instance_model`/`place_model` (cached PackedScene) helpers; the Mos Eisley settlement now parks real Kenney low-poly **craft on Bays 86/87 + by the speeder shop** (Bay 94 left clear for the range), uses **factory-kit crate models** for the crate stacks (box collision preserved), and scatters **survival-kit barrels**. All additive/visual-only — existing collision + layout untouched — and shared by both the solo (`main.tscn`) and networked worlds. Model scales/orientations are first-pass: **an owner visual check is worthwhile.** Building-model swaps + scale tuning are queued as **A1b** (after M2).

**M1.4 (persistence backbone) is DONE.** New pure `scripts/net/persistence_store.gd` saves/loads one JSON record per character (shape per `data/schemas/player_persistence.schema.json`; only fields we have now are populated — position + CP/FP/wound + name) under `user://persistence`, covered by `persistence_smoke`. `NetworkManager` gained a `register_account(account_id)` RPC that loads/restores the character on login (server-authoritative position via `WorldState.restore_player`, combat via `CombatArena.set_player_combat`), saves on disconnect, and autosaves every 30s; the client passes `--account <id>` (+ a `--autowalk` headless test affordance). **Verified end-to-end**: a client autowalked from spawn to the world edge (z=-58), disconnected, and a reconnect with the same account was restored to (-20, 1.2, -58). Full `check_project.ps1` green (now 33 GDScript smokes + python). **M1.5 (player identity/nameplate) is DONE.** Clients pass `--name <name>`; it flows via `register_account(account_id, display_name)` to the WorldState player name (snapshot nameplates), the CombatArena player name (combat envelopes show the chosen name, not `Spacer-N`), and the persisted record. Covered by net_smoke (restore_player rename) + combat_arena_smoke (named shooter); verified over the wire (`--name "Mara Jade"` → `Mara Jade hit B1 ...`). Full gate green (33 GDScript smokes + python). **M2.0 (zone & security-state scaffold) is DONE — the persistent world sim begins.** New pure `scripts/net/zone_state.gd` tracks per-zone faction influence (republic/cis/hutt/independent, non-zero-sum) with a DERIVED alert level and DERIVED effective security tier (Hutt≥80 downgrades a tier; a republic_crackdown event upgrades contested→secured), advanced by a **slow deterministic 30s Director tick** (decay toward baseline) that is wholly separate from the 20 Hz movement tick. **No LLM** — the optional Director "flavor" layer is an open owner decision, left off. The server seeds a Mos Eisley spaceport zone and folds its posture into the broadcast snapshot; the client renders an alert/security badge. Covered by `zone_state_smoke` + a two-process check (`zone=high_alert/secured`). Full gate green (34 GDScript smokes + python). **M2.1 (territory-claim scaffold) is DONE.** New pure `scripts/net/territory_model.gd`: an org claims a node in a contested/lawless zone (precondition: zone influence ≥ a foothold floor; secured zones not claimable; one claim per node), deriving an influence tier (foothold/dominant/control) and member effective-security (a lawless node upgrades to contested for owning members). Claims accrue passive income to org treasuries on a 60s resource tick, scaled by tier × risk (lawless yields more, rarer resources). Covered by `territory_smoke`. `NetworkManager` holds the registry + resource tick (a no-op until the org/claim command layer exists). **The siege / Drop-6D hostile-takeover loop is deliberately NOT built — its tuning is an open owner decision.** Full gate green (35 GDScript smokes + python).

**Loop reprioritization:** the remaining queued items split into (a) **content drop 2** (vehicles/starships + droids data — fully gate-verifiable, good for unattended) and (b) **visual items A1b/P1** (building-model scale-fitting, client polish) which produce results only a human can judge. The loop will do content drop 2 next and **defer A1b/P1 until an owner visual check** of the current low-poly world. After content drop 2, the highest-value remaining features (the org/claim command layer + guard NPCs, the siege loop, Force/Jedi, the death-penalty implementation) increasingly need owner design calls — so the loop will trend toward its "needs owner" stop condition.

**Wave C (Character Creation & Progression) is in progress — C1 DONE.** New pure `scripts/rules/chargen_model.gd` implements WEG R&E chargen: `validate_build` enforces exactly 18D of attribute dice within the species min/max ranges plus a 7D skill-dice budget, and produces a starting `sheet` (CP 5 / FP 1 / `force_sensitive` false / `healthy`) in the persistence shape; `default_build` gives a deterministic in-range quick-start. Covered by `chargen_smoke`. Full gate green (36 GDScript smokes + python). **C2 (server chargen flow) is DONE:** `register_account(account_id, name, build)` runs chargen for NEW characters — `_create_character` validates the requested WEG build (or a deterministic quick-start) against the server-loaded species data and persists the sheet; the client passes `--species`/`--quickstart`. Verified over the wire (a new `--species rodian --quickstart` account created a species-aware `dex=3D+1, cp=5` and persisted). Existing characters load unchanged. (Combat still uses the shared trainee pools — wiring the per-character sheet into combat is a later slice.) **C3 (progression model) is DONE:** pure `scripts/rules/progression_model.gd` (WEG R&E / Guide_09) — `skill_raise_cost` = the total-pool dice count (attribute + skill bonus), stepping up at die boundaries, with an optional guild discount; and the DIV-0007 DUAL-TRACK CP wallet `{gameplay_cp, rp_cp}` with `raise_skill` (adds a pip to the skill bonus, spends gameplay-first then RP-prestige, rejects when short) + `award`. Covered by `progression_smoke`. Full gate green (37 GDScript smokes + python). **C4 is DONE — WAVE C (Character Creation & Progression) is COMPLETE.** Disabling the shared combat target awards gameplay CP to the shooter's persisted `sheet.cp_wallet` (`_award_cp`, `COMBAT_CP_REWARD=3`, tunable); a `submit_skill_raise(skill)` RPC validates the spend via `progression_model` against the character's wallet, governing attribute (looked up from the skill catalog), and current bonus, applies the new bonus to the sheet, persists, and replies; the server pushes the wallet to the client via `apply_wallet`. The client renders a CP HUD and raises Blaster with `K` (or `--raise-skill` headless). Verified over the wire: a new quickstart character earned CP from kills and raised `blaster 0D -> 0D+1 (cost 3)`, persisted. Full gate green (37 GDScript smokes + python).

**Wave D (combat uses the character sheet) followed Wave C; D1 is DONE.** `scripts/net/combat_arena.gd` now builds each player's combat pools from their persisted sheet (attack = DEX + blaster bonus, dodge = DEX + dodge bonus, soak = STR; the target side stays shared; damage uses a default starter blaster until an inventory exists). `register_player`/`set_player_sheet` take a sheet (no sheet = trainee fallback, backward-compatible); `NetworkManager` applies it on login and re-applies on each skill raise (so a raise strengthens your attack immediately). Verified over the wire: a new quickstart character fought with attack pool DEX 3D, and raising Blaster grew it to 3D+1. Full gate green (37 GDScript smokes + python).

**Unattended driver change (2026-06-25):** the loop is now driven by a recurring **CronCreate** timer (~every 10 min, fires only when idle so no overlap; session-bound, 7-day auto-expire) instead of manual `/loop` — and runs WITHOUT owner questions (engineer-level decisions; owner-gated forks parked as BLOCKED-needs-owner). **D2 (combat uses equipped weapon + armor) is DONE.** Chargen sheets now carry a starter `equipment {weapon: blaster_pistol, armor: blast_vest}`; `combat_arena` takes weapon/armor catalogs and builds each player's `damage_pool` from the equipped weapon and `player_armor` from the equipped armor (fallbacks when absent); `NetworkManager` loads `weapons_clone_wars.json` + `armor_clone_wars.json` and passes them in. `chargen_smoke`/`combat_arena_smoke` extended (equipped heavy blaster → 5D damage; no equipment → default). Full gate green (37 GDScript smokes + python). Combat now reflects the full character: attributes, skills, AND gear. Known follow-up (D3): an inventory/equipment-swap system so players can change loadout. NOTE: a parallel session is concurrently working the asset pipeline (`docs/ASSET_PIPELINE.md`, `assets/`); my Wave C work is in `scripts/` and stays scoped to avoid collisions. **M2.2 (Director world events) is DONE (self-extended world-sim).** `scripts/net/zone_state.gd` now fires one deterministic world event at a time per zone — when no event is active it rolls `hash(tick:zone) % 100` against `EVENT_CHANCE` and, on a hit, picks a type from a **fixed 12-event menu** keyed off the zone's dominant influence (republic crackdown/checkpoint, hutt bounty/auction, cis propaganda/pirate, else a neutral Tatooine list: merchant caravan, sandstorm, cantina brawl, distress signal, krayt sighting, trade boom). Each event carries a Clone Wars headline, lasts `EVENT_DURATION` slow ticks, and surfaces in `zone_summary` as `event`/`event_type`; the client renders a **NEWS** HUD line and logs `[news] <headline>` on change. **Still no LLM** — the optional Director "flavor" layer stays the open owner decision; this is the deterministic baseline. A `--director-tick <secs>` server override drives the slow tick fast for headless verification. Covered by an extended `zone_state_smoke` (an event fires within 40 ticks, is from the menu, and is deterministic); verified over the wire (a client received neutral Tatooine headlines streaming from the server). Full gate green (37 GDScript smokes + python).

**Clean-session handoff + Wave E stocked (2026-06-25).** Ahead of the owner being away for an all-day unattended push, ran a 5-reader **Workflow** audit (rules / netcode / world-sim+data / tests / docs, ~560k tokens) over the whole prototype and synthesized a deep, breadth-first backlog — `docs/UNATTENDED_BACKLOG.md` → **Wave E (E1–E27)** — explicitly tagged `[PAR]` (parallel-safe pure-model+test/docs slices) vs `[HOT]` (serialize on `network_manager.gd`/`net_world.gd`) so the loop can batch parallel work via Workflow and serialize hot-file work. Wrote `docs/SESSION_HANDOFF.md` as the canonical clean-session entry point (current state + the re-armable all-day loop contract + the parallelization playbook + guardrails + the owner-gated park list); added a "read SESSION_HANDOFF first" pointer to `CLAUDE.md` and refreshed its stale milestone section. **Key mechanism note:** the CronCreate driver is *session-only* and does not survive a session switch, so the handoff's first action is for the new session to **re-arm its own cron** (≈10-min, idle-only) from the embedded contract. Retired the prior session's stale cron to avoid a double-driver. Highest-value Wave E threads: restore the WEG cumulative wound ladder + Wounded-Twice tier (E2, ledger-first), wound recovery/healing (E3), the player→world influence loop (E8→E24), the org claim command layer (E9→E23), and finally consuming the orphaned creatures/droids data (E10/E11). Docs-only + backlog changes — gate unaffected and green.

**Wave E loop resumed (2026-06-25).** A fresh clean session re-armed the ~10-min idle-only CronCreate driver from `docs/SESSION_HANDOFF.md` (the prior session's cron died with it) and confirmed the full gate green at HEAD `5ba15f0` before touching anything. **E1 (reconcile stale roadmap docs) is DONE** — docs-only: advanced `MULTIPLAYER_FOUNDATION.md` Status/Roadmap (M1.3 DONE; M1.3b/M1.4/M1.5/M2.0–2.2/Wave C/Wave D added; Wave E in-progress), deleted the self-contradicted "loop has STOPPED at Wave C" sentence here, archived `NEXT_DECISIONS.md`, and corrected the stale "`--import` can fail" caveat in `UNATTENDED_LOOP.md` + the backlog Guardrails (A0's colormap fix is live; the full `check_project.ps1` is the bar). No code touched.

**Wave E [PAR] batch 1 DONE (2026-06-25): E4 + E10 + E11 + E12.** Four independent pure-model slices authored concurrently via a Workflow (one author→adversarial-review pipeline per slice, 8 agents, ~348k tokens), each creating only its own NEW files on the main tree, then integrated serially with ONE full gate. **E4** `scripts/rules/derived_stats_model.gd` — WEG derived stats (species Move, base soak = STR, melee STR-bonus, stun-knockout threshold = STR dice). **E12** `scripts/rules/reputation_model.gd` — per-character reputation on 5 signed faction axes (republic/cis/hutt/independent/bounty_hunters_guild), non-mutating apply_delta, standing tiers, serialize. **E10** `scripts/rules/creature_spawn_model.gd` — finally CONSUMES the orphaned `creatures_clone_wars.json`: seed-driven deterministic spawn rolls with zone-posture bias (dangerous→hostile, calm→non-hostile). **E11** `scripts/rules/vendor_model.gd` — finally CONSUMES `droids_clone_wars.json` bargain tiers + vendor-stocked weapons/armor: deterministic price model (Director event multiplier × bargain-droid discount). Each has a wired smoke; full gate GREEN (now 38 GDScript smokes + 7 python + import + launch). The static adversarial reviewers passed two GDScript `:=`-type-inference parse errors (assigning an untyped `rules`/`model` call result with `:=`); the FULL GATE caught both and they were fixed to explicit `: Dictionary` — a reminder that the Godot gate, not static review, is the authoritative bar.

**Wave E [PAR] batch 2 DONE (2026-06-25): E7 + E8 + E9 — the world-sim pure substrate.** Three pure models in `scripts/net/*` that unblock the [HOT] features, authored concurrently via a Workflow (author→adversarial-review, 6 agents); the batch-1 `:=`-trap lesson was fed to the agents as an explicit warning and all three smokes passed standalone on the first try. **E7** `security_gate.gd` — pure `get_effective_security` implementing WORLD_SIM_DESIGN §3.2's single-gate precedence (room override → citizen one-step upgrade as a safety floor → claim lawless→contested → Director Hutt-surge/crackdown overlay), the consolidated combat-security resolver (pure; the live-attack-path wiring is deliberately left for a [HOT] slice). **E8** `pending_influence_model.gd` — non-mutating accrual + fold-and-clear of `world_hooks.pending_zone_influence`, the substrate for the player→world influence loop ([HOT] E24). **E9** `org_model.gd` — membership validation (one faction + ≤3 guilds, rank gates 3=claim/5=city) + `can_claim_command` composing rank + zone security + influence floor by REUSING `territory_model`'s constants (one source of truth), the substrate for the org claim-command RPCs ([HOT] E23). Full gate GREEN (now 41 GDScript smokes + 7 python + import + launch). Session tally so far: E1 + 7 Wave E pure/test slices done across two parallel batches; the [PAR] substrate for E23/E24 is now in place.

**Wave E rules batch 3 DONE (2026-06-25): E2 + E3 — WEG wound ladder + recovery (ledger-first).** The handoff's flagged highest-value thread, authored by the **d6-rules-engineer** subagent (sequential workflow, adversarial review each). **E2** restored the canonical WEG cumulative wound ladder in a new pure `wound_ladder_model.gd` (Wounded-Twice −2D tier, total `escalate()`), and fixed the long-standing silent collapse where `ground_combat._wound_penalty_dice` returned 0D for severity ≥ 3 — now it delegates to the ladder (sev3 → −2D). The `ground_combat_model.gd` edit was scoped to ONLY that one function body + a preload const, so `ground_combat_model_smoke`/`combat_arena_smoke`/`net_smoke` stayed green (verified by the full gate). **E3** added a pure `recovery_model.gd` — stun auto-expiry, First Aid/Medicine heal checks vs the Guide_19 §3 difficulty table, the Mortally-Wounded 2D death roll, and the post-death −1D debuff. **DIV-0008** (penalty collapse restored; `escalate()`-into-live-combat is a tracked follow-up) and **DIV-0009** (recovery timing/roll approximations) document the divergences. The E3 reviewer caught + fixed a genuine `:=`-on-preloaded-const parse trap. Full gate GREEN (now 43 GDScript smokes + 7 python + import + launch). Session tally: E1 + 9 Wave E slices across three batches; combat fidelity (E2) + the social/world-sim substrate (E7/E8/E9) advanced.

**Wave E rules batch 4 DONE (2026-06-25): E5 + E6 (ledger-first).** Two additive, backward-compatible rules edits authored concurrently by the **d6-rules-engineer** subagent (disjoint files: `d6_rules.gd`+`rules_smoke.gd` vs `chargen_model.gd`+new Force files), integrated with one full gate; every affected smoke stayed green. **E5** restored per-weapon range bands — `d6_rules.range_band_for_weapon()` reads each weapon's own `[short_min,short_max,med_max,long_max]` and is threaded through `resolve_ranged_attack` via a FINAL optional `weapon_ranges` arg, so existing combat callers are byte-identical (the fixed `RANGE_BANDS` table stays as the fallback; wiring live combat to pass weapon ranges is a tracked follow-up). **E6** landed the off-by-default WEG Force-skill data hook (`force_skills_model.gd`: control/sense/alter at 0D, inactive unless `force_sensitive`; chargen carries the hook) with NO power list or access policy — Force access & scarcity stay an OPEN OWNER DECISION. **DIV-0010** (range bands) + **DIV-0011** (Force access owner-gated) document both. Full gate GREEN (now 44 GDScript smokes + 7 python + import + launch). **Session tally: E1 + 11 Wave E slices across four batches** (E1,E2,E3,E4,E5,E6,E7,E8,E9,E10,E11,E12 done), DIV-0008..0011 added; combat fidelity (E2/E5), recovery (E3), the social/world-sim substrate (E7/E8/E9), and the off-by-default Force hook (E6) all in. Remaining Wave E: E13 (Director event effects), E14 (data-driven set-dressing), E15–E20 (test-only coverage), and the [HOT] netcode features E21–E27 (one at a time, two-process verified). The driver cron continues the loop.

**Wave E test-coverage batch 5 DONE (2026-06-25): E15–E20.** Six regression-guard smokes authored concurrently by the **gdscript-test-author** subagent (author→adversarial-review, 12 agents), each cross-checking its asserted literals against the real source; all six passed standalone on the first try. E15 `snapshot_merge_smoke` (the WorldState+zone merge clients consume), E16 `wire_roundtrip_smoke` (JSON RPC-payload fidelity — reviewer confirmed Vector3→JSON behavior against Godot 4.6 `json.cpp`), E17 `skill_attribute_smoke` (the 16 skill→attribute maps + default, locking the server skill-raise lookup), E18 `character_lifecycle_smoke` (chargen→persist→reload parity), E19 `cp_award_smoke` (the award→spend economy hook via pure progression_model), E20 hardened `combat_event_log_model_smoke` (trim-keeps-newest + chronological filter). Full gate GREEN (now **49 GDScript smokes** + 7 python + import + launch); test-only, no source touched. **Session tally: E1 + 17 Wave E slices across five batches** — E1–E12 + E15–E20 DONE; DIV-0008..0011 added; gate grew 34→49 smokes, green throughout. **Remaining Wave E for the cron loop: E13 (Director event effects/influence nudges, edits `zone_state.gd`), E14 (data-driven `world_builder` set-dressing), and the [HOT] netcode features E21–E27 (`network_manager.gd`/`net_world.gd`, ONE at a time, each two-process verified).** The [PAR] substrate the [HOT] features consume (E7/E8/E9 + the wound/range/Force rules) is in place.

**Wave E world-sim batch 6 DONE (2026-06-25): E13 + E14 — the last [PAR] substrate.** **E13** closed the documented event→world causal loop in `zone_state.gd`: a bounded `EVENT_EFFECTS` table (smuggling/vendor/spawn/perception per event, surfaced in `zone_summary["effects"]`) plus per-tick active-event influence nudges (republic/cis/hutt) capped at `EVENT_INFLUENCE_CAP=69` (one below lockdown/underworld) so events build a foothold but extreme dominance stays player-driven — the cap is exactly what keeps the existing surge-decay smoke green (a surge already above it just decays). **E14** (lowpoly-scene-builder) added data-driven set-dressing: `data/mos_eisley_props.json` (props referencing only existing GLBs) read by `world_builder._place_data_props()` with a graceful absent/malformed fallback, shared by solo + net. Both extend their already-wired smokes; full gate GREEN (49 smokes). **With E13/E14 done, the entire Wave E [PAR]/[PAR*] substrate (E1–E20 + E13/E14) is COMPLETE; only the [HOT] netcode features E21–E27 remain** — and the pure substrate each one consumes is ready (E21 multi-zone, E22 equip/D3, E23 org-claim RPCs←E9, E24 player→influence←E8, E25 chat, E26 account-auth, E27 ambient NPC sim←E10/E21). Do them ONE at a time on the main tree with a two-process headless check each.

**Wave E [HOT] #1 DONE (2026-06-25): E21 — multiple zones + per-peer snapshot routing.** The first [HOT] slice (solo on the main tree). `network_manager.start_server` now seeds its zone roster from new `data/zones_clone_wars.json` (4 zones — spaceport secured, port-fringe + market contested, dune-sea lawless; graceful fallback to the single hardcoded zone) and the Director ticks them all. Each peer carries a `current_zone_id` (`_peer_zones`, default on connect, optionally chosen via a `zone` key on the existing `register_account` build dict so the RPC signature is unchanged); `_build_snapshot(zone_id)` emits that player's zone and the 20 Hz broadcast became a per-peer `apply_snapshot.rpc_id` loop. Client gains `--zone`. New `zones_smoke`; full gate GREEN (50 smokes). **Two-process verified:** server seeded 4 zones + routed a `--zone tatooine.dune_sea` client, which received `zone=standard/lawless` snapshots — a contested/lawless zone is now live (the precondition E23 territory claims and E27 ambient sim need). **Remaining [HOT]: E22 (equip/D3), E23 (org-claim RPCs←E9), E24 (player→influence←E8), E25 (chat), E26 (account-auth+rate-limit+cache), E27 (ambient NPC sim←E10/E21).**

**Wave E [HOT] #2 DONE (2026-06-25): E22 — inventory / equipment-swap RPC (D3).** Completed the half-wired equip path. New pure `equipment_model.gd` validates a swap (slot + catalog + ownership against `sheet.inventory`, with a fallback owning equipped gear); chargen sheets carry a starter inventory; `network_manager.submit_equip` loads→validates→writes `sheet.equipment`→persists→`arena.set_player_sheet`→replies (mirrors submit_skill_raise); `net_world` gains `--equip <slot:item>`. New `equip_smoke`; full gate GREEN (51 smokes). **Two-process verified:** an unowned `blaster_rifle` was rejected `(not_owned)`, an owned `hold_out_blaster` swap was accepted (damage pool 4D→3D+1) and survived reconnect (`[weapon=hold_out_blaster]`). Item acquisition (vendor/loot) to grow inventory beyond the starter set is a known follow-up. **Remaining [HOT]: E23 (org-claim RPCs←E9), E24 (player→influence←E8), E25 (chat), E26 (account-auth+rate-limit+cache), E27 (ambient NPC sim←E10/E21).**

**Wave E [HOT] #3 DONE (2026-06-25): E23 — org claim/release command RPCs.** Wired the live `Territory` into the RPC surface: `submit_claim_node` / `submit_release_claim` validate via E9's `OrgModel.can_claim_command` (member + rank≥3) + `TerritoryModel` (zone claimable + influence floor + one-per-node), and the 60s resource tick now credits the org treasury (no longer a no-op). Org membership + per-zone territory influence are seeded via a `build.org` test affordance (real faction-join + influence accrual are later features); a treasury/claims summary folds into the per-peer snapshot. The two-process check caught a real-only bug — JSON reload widens `faction_rank` to float, tripping org_model's strict int check — fixed by coercing in `_org_for_peer` (a reminder that pure smokes can't catch persistence-type drift). Full gate GREEN (51 smokes). **Two-process verified:** a rank-3 Hutt member claimed a lawless node (foothold) and accrued 150 credits/tick; secured-zone `(secured_zone)`, rank-2 `(rank)`, and already-claimed `(node_unavailable)` all rejected. **Remaining [HOT]: E24 (player→influence←E8), E25 (chat), E26 (account-auth+rate-limit+cache), E27 (ambient NPC sim←E10/E21).**

**Wave E [HOT] #4 DONE (2026-06-25): E24 — player actions feed zone influence.** Closed the player→world loop (server-only). On a combat target-disable, the shooter's faction axis accrues `DISABLE_INFLUENCE=5` into a pending buffer (E8 `PendingInfluence.add_pending`); `_fold_pending_influence` folds-and-clears it into `zones.apply_influence_delta` just before each Director tick, so play shifts faction influence which then decays/re-derives. Reuses the E8 model + E13 nudges; no new smoke. Full gate GREEN (51 smokes). **Two-process verified:** an autofire republic player disabled the target 9× → republic influence 55→90 and at the 70 threshold the **alert flipped high_alert→lockdown** (carried in the per-peer snapshot). **Remaining [HOT]: E25 (chat), E26 (account-auth+rate-limit+cache), E27 (ambient NPC sim←E10/E21).**

**Wave E [HOT] #5 DONE (2026-06-25): E25 — chat / emote (first social channel).** New pure `chat_model.gd` (channel whitelist say/emote/ooc + control-char strip + 256 clamp + empty reject + per-channel `format_line`); `network_manager.submit_chat` validates with the player's display name and broadcasts `apply_chat` to all peers; `net_world` gets a HUD chat panel + `--chat <channel:text>`. New `chat_model_smoke`; full gate GREEN (52 smokes). **Two-process verified:** ObiWan's emote round-tripped to BOTH peers, and a bogus channel was rejected `(bad_channel)`. **Remaining [HOT]: E26 (account-auth + rate-limit + cache), E27 (ambient NPC sim←E10/E21).**

**Wave E [HOT] #6 DONE (2026-06-25): E26 — account auth/ownership guard + rate-limit + record cache.** Closed the identity-spoofing gap. New pure `account_auth_model.gd` (`check_secret` ownership guard + a `consume_token` token bucket). `network_manager`: register enforces the account secret BEFORE binding/loading (wrong secret rejected, no overwrite; first claim persists `account_secret`); a read-/write-through `_record_cache` replaces all per-call `load_record`/`save_record` (skill-raise et al. no longer re-read JSON); a `_rate_ok` token bucket throttles all 7 reliable RPCs (not movement); `net_world` gains `--secret`. New `account_auth_smoke`; full gate GREEN (53 smokes), persistence/net/combat unregressed. **Two-process verified:** A claimed `e26vault/swordfish`, B with `guess` was denied `(bad_secret)` and couldn't overwrite, C with `swordfish` loaded; no throttles on legit play. **Remaining [HOT]: E27 (ambient NPC sim←E10/E21) — the LAST Wave E item.**

**Wave E [HOT] #7 DONE — and WAVE E COMPLETE (2026-06-25): E27 — ambient NPC sim + snapshot.** New pure `ambient_sim_model.gd` deterministically maintains a small per-zone NPC roster keyed to alert (hash-seeded, in-bounds, lifespan + despawn-on-expiry); `network_manager` advances it on the Director tick and folds `npcs[]` into the per-peer snapshot. New `ambient_sim_model_smoke`; full gate GREEN (54 smokes). **Two-process verified:** the server populated all 4 zones (spaceport 4 / others 3, stable) and a client's snapshots carried `npcs=4`. **With E27, the entire Wave E queue (E1–E27) is DONE** — across this all-day session: E1 + the [PAR] substrate (E2–E20, E13/E14) + the [HOT] tier (E21–E27, all two-process verified), DIV-0008..0011, the gate grown 34→54 smokes and green at every commit. The persistent player-driven MMO loop is live end-to-end. **The backlog is now DRY of unblocked, non-owner-gated work** — the next session needs an owner steer (a new wave) or the parked visual/design forks (siege tuning, Force access, PvP-consent, LLM-Director, death-penalty numbers, CP rates, A1b/P1).

**Post-Wave-E hardening (2026-06-25, review-driven).** With the backlog dry, ran a 4-dimension adversarial code review of the Wave E [HOT] netcode and fixed the 2 confirmed MEDIUM latent bugs in `register_account` (both invisible to the stock once-per-session client): stale `_peer_orgs`/`_peer_axes` on a no-org re-register (now cleared), and a missing single-session lock letting two authorized peers clobber one character (now rejected `already_logged_in`). The 6 determinism findings were correctly rejected (GDScript dict iteration is insertion-ordered; sims are seeded/order-independent). Full gate GREEN (54 smokes); single-session lock two-process verified.

**Wave-E follow-up F1 (2026-06-25): territory-influence accrual from combat.** Completed the documented E23 follow-up — a kill-in-zone now earns the shooter's org `+2` territory influence (FACTION_TERRITORY_DESIGN §2), so claims are EARNABLE through play instead of test-seeded. Closes the persistent-world loop end-to-end: kills → org territory influence → claim at the floor → treasury income. Full gate GREEN (54 smokes); two-process verified (an early claim denied `(influence)`, then influence accrued 6→12→18→24 past the 20 floor). Remaining follow-ups need an owner steer: presence/mission/PvP influence sources (hourly-presence timescale is an owner call), vendor-purchase acquisition (needs a starting-credits/economy call), zone-scoped say.

**Wave-E follow-up F2 (2026-06-25): zone-scoped say.** `submit_chat` now delivers `say`/`emote` only to same-zone peers (proximity chat) while `ooc` stays global — the documented E25 refinement. Delivery-only (chat_model untouched); full gate GREEN (54 smokes); two-process verified (a spaceport `say` did not reach a dune_sea peer; an `ooc` did). **With F1+F2, the cleanly-non-owner-gated Wave-E follow-ups are now exhausted** — the remaining ones (vendor-purchase acquisition / starting-credits economy, presence-influence timescale) need an owner steer, alongside the parked owner-gated/visual forks. The prototype is feature-complete for Wave E with all core loops playable + hardened.

**Wave-E follow-up F3 (2026-06-25): claim-flow regression guard.** Added `claim_flow_smoke` — a headless gate guard (E15-E20 replicate-the-wiring pattern) for the `submit_claim_node` validation precedence (no_org→rank→secured_zone→influence→node_unavailable), which was previously only two-process verified. Composes the real OrgModel + TerritoryModel; test-only; full gate GREEN (55 smokes). **With Wave E + hardening + F1/F2/F3, the non-owner-gated work is genuinely complete** — the prototype is feature-complete, hardened, with the core loops playable AND gate-guarded. Anything further (vendor/economy, presence-influence timescale, reputation wiring) needs an owner steer, alongside the parked owner-gated/visual forks. The loop is holding at this clean state for owner direction. With E21–E24 done, the persistent player-driven world loop is live end-to-end: multiple zones → per-player routing → equip + claim commands → and player activity reshaping faction influence/security.

**Wave-E follow-up F4 (2026-06-25): register-account regression guard.** Added `auth_flow_smoke` — a headless gate guard (claim_flow/E15-E20 replicate-the-wiring pattern) for the `register_account` composition precedence (`rate-limit → auth(secret) → single-session lock → bind → org set/CLEAR`), the exact wiring where adversarial review found the 2 MEDIUM latent bugs and which was previously only two-process verified. Composes the real `AccountAuthModel`; asserts auth-precedes-lock, the single-session lock (BUG #2: a second peer on an owned char is denied `already_logged_in`, a different char / same-peer re-bind allowed), the org set-then-CLEAR on re-register (BUG #1: a no-org re-register clears `_peer_orgs`/`_peer_axes`, no stale faction), and rate-limit-before-auth. Test-only; full gate GREEN (56 smokes). **With F4 the two confirmed hardening fixes are now gate-guarded, not just two-process verified** — closing the regression gap the hardening pass left open.

**Wave-E follow-up F54 (2026-06-25): UX — /help also lists the KEYBINDS [HOT, client-only].** Onboarding gap: the action keys grew (X cover, Z dodge, G full-dodge, C/F boost, H heal, K raise, V sheet, T travel) and the non-obvious ones weren't discoverable anywhere. `/help` now also prints a `Keys: …` line (+ shows it in the status), alongside the chat/command list. Doesn't touch `command_help()` (F42 guard unaffected). Full gate GREEN (59 smokes). **Two-process verified:** `/help` logged both `[help] Commands: …` and `[help] Keys: WASD move · …`.

**Wave-E follow-up F53 (2026-06-25): info — surface the ONLINE org member count in the org HUD [HOT].** Diversifying back to faction legibility (cf. F34/F35/F36). The org HUD lacked the org's online presence — for a faction MMO, knowing how many org-mates are online (cross-zone) is coordination info the server already tracks in `_peer_orgs`. The snapshot `territory` block now carries `org_members_online` (scoped to the viewer's org); the org HUD shows "· N online" + logs `[org] … members=N`. Pure display; F29 claim readout preserved. Full gate GREEN (59 smokes). **Two-process verified (3 clients):** two Hutt members each read `members=2`, a lone Republic member read `members=1`.

**Wave-E follow-up F52 (2026-06-25): gameplay — expose ACTIVE DODGE, completing the WEG defense-stance set [HOT, rules+netcode].** The middle stance (none/dodge/full-dodge): attack AND actively dodge at a -1D multi-action penalty (`DEFENSE_DODGE`). Was in the model but unreachable. `submit_fire_intent` carries `dodge`; `_apply_intent` routes to it; a `Z` toggle + `--fire-dodge` affordance; the attack line shows "(dodging)". Build-relevant (high-dodge characters). Gate-covered (combat_arena_smoke: defends return fire `defense_type "dodge"` + player_attack `action_count 2`). Full gate GREEN (59 smokes). **Two-process verified:** `hit B1 … → Wounded (dodging)`. **Combat tactical loop now COMPLETE:** attack (aim/CP/FP/cover) · dodge · full-dodge + Perception initiative (F48) + wound/recovery (F44).

**Wave-E follow-up F51 (2026-06-25): gameplay — expose the DEFENSIVE STANCE / full dodge [HOT, rules+netcode].** Completes the offense/defense loop + pairs with the medical loop (wounded → defend → recover). The model fully supports full-dodge (`DEFENSE_FULL_DODGE` → `_resolve_full_dodge_exchange`) but `combat_arena` hardcoded `player_defense = DEFENSE_NONE`. Now `submit_fire_intent` carries `full_dodge` and `_apply_intent` routes to it; a `G` key + `--autodefend` affordance submit a defend intent; the combat log shows "<shooter> takes a defensive stance (full dodge)". Gate-covered (combat_arena_smoke: full_dodge emits `player_full_dodge`, no target damage). Full gate GREEN (59 smokes). **Two-process verified:** stance shown, target stays severity 0.

**Wave-E follow-up F50 (2026-06-25): gameplay — expose the COVER defensive option [HOT, client-only].** Newly relevant since F44 (the dummy returns fire). The fire intent's `cover` field was hardcoded 0, so the player had no defense. Added an `X` toggle + `--fire-cover` affordance; the F45 line shows "(in cover)". A two-process check caught a misleading 0-4 representation (`--fire-cover 4` → `(cover 1)`) because `ground_combat_model` clamps cover to `COVER_QUARTER (1)` when you attack (WEG: firing exposes you) — so I converted it to an honest 1/4-cover **toggle**. Full gate GREEN (59 smokes). **Two-process verified:** `takes return fire (in cover) → Wounded`.

**Wave-E follow-up F49 (2026-06-25): VALIDATION — multi-player sparring works end-to-end (docs-only).** Verified the complete combat system (F44 two-way damage + F45–F47 feedback + F48 Perception initiative) composes for MULTIPLE players. Two-process (2 autofire clients on the shared B1 dummy): server saw `2 shot(s)` per window (multi-shooter, initiative-ordered), both clients climbed `stunned → wounded (-1D)` (dummy returns fire at each), and the non-lethal cap held for BOTH (neither incapacitated). No bug, no code change. Multi-player PvE-lite combat + medical loop confirmed solid.

**Wave-E follow-up F48 (2026-06-25): fidelity — wire the character's PERCEPTION into combat initiative [rules, combat_arena].** WEG: initiative = a Perception roll. The arena hardcoded `{dice:3}` for every player, ignoring the attribute (an unflagged simplification, now meaningful with the F44 two-way multi-shooter exchange). `_pools_from_sheet` now builds a `perception_pool` from `sheet.attributes.perception`; `_initiative_order` uses it; no-sheet fallback keeps the old fixed 3D. Restores fidelity, not a new divergence. Pure-model, gate-covered (combat_arena_smoke: pool = sheet's Perception; a 6D shooter resolves before a 1D one). Full gate GREEN (59 smokes). **Two-process (no-regression):** live sparring unaffected.

**Wave-E follow-up F47 (2026-06-25): info — persistent at-a-glance TARGET status readout [HOT, client-only].** Completes the combat-feedback set (F45 return fire + F46 own condition). The combat log only showed the target's wound on a HIT; now a persistent `_target_label` ("Target: B1 Training Silhouette — Wounded") tracks the shared target from the envelope's `target_name`/`target_wound_severity` (severity → Healthy/Stunned/Wounded/Disabled), logging `[target] <name> <cond>` on change. Pure presentation, forward-looking for real targets/PvP. Full gate GREEN (59 smokes). **Two-process verified:** tracked `[target] … Healthy → Disabled → Stunned → … → Healthy` across hits/misses + the respawn cycle.

**Wave-E follow-up F46 (2026-06-25): info — surface the WEG wound PENALTY in the condition HUD [HOT, presentation].** Newly meaningful after F44. The wound penalty (stunned/wounded −1D, wounded_twice −2D) is genuinely applied to the player's attack/dodge/soak in live combat but was invisible. The `you` snapshot block now carries `wound_penalty` (from `WoundLadderModel.penalty_dice_for_level`, source of truth); the condition HUD shows `Wounded (-1D to actions)` and logs `[condition] you=wounded penalty=-1D`. Pure presentation. Full gate GREEN (59 smokes). **Two-process verified:** `[condition] you=wounded penalty=-1D`.

**Wave-E follow-up F45 (2026-06-25): info — surface the remote's RETURN FIRE in the combat log [HOT, client-only].** Newly relevant after F44 (combat now wounds players). The client only rendered the player's attack on the target, so a sparring-wounded player's condition flipped silently. The exchange already emits a `remote_return_fire` event + the envelope's `state_delta.player_wound_severity` carries the capped resulting wound (the arena clamp mutates result.state in place before the envelope is built, so it matches the condition HUD). `_on_combat_envelope` now appends "<shooter> takes return fire → <wound>". Pure presentation. Full gate GREEN (59 smokes). **Two-process verified:** `[combat] Spacer-… takes return fire → Wounded`. Completes the combat-feedback loop (shoot → get shot back → wounded → recover).

**OWNER-DIRECTED F44 (2026-06-25): the MEDICAL LOOP is now LIVE — non-lethal sparring damage [HOT, rules+netcode, DIV-0016].** Owner pressed to open the highest-ROI gate. The built-but-dark wound/recovery/First-Aid/condition system is now reachable: the B1 training remote returns REAL fire, capped non-lethal. (a) `ground_combat_model._resolve_return_fire` stun_mode is data-driven (`target_stun_mode`, default true = unchanged); the sparring remote sets `stun_return_fire:false`; `combat_arena.resolve_window` clamps the player at `SPARRING_MAX_SEVERITY=2` (Wounded, never incapacitated/3+ → no death/respawn). (b) Heal-sync fix: `submit_heal`+`_recover_wounds` read the autosave-lagged sheet, so combat wounds were untreatable (`no_wound`); new `_live_wound_state` makes both heal paths use the live arena wound. Designed via a 7-agent understand→design→synthesize Workflow (caught that return fire already existed + propagated). Full gate GREEN (59 smokes; combat_arena_smoke extended w/ the cap proofs). **Two-process verified end-to-end:** condition climbed stunned→wounded, never incapacitated; natural recovery healed it (`[recovery] wounded→stunned→healthy`); a medic First-Aided it (`[firstaid] healed wounded→stunned`). DIV ledger now 0001..0016.

**Wave-E follow-up F43 (2026-06-25): docs — owner-facing gate→unlock ROI map (SESSION_HANDOFF §5a).** The non-gated backlog is dry and the loop built the substrate ahead of the gates, so a lot of already-built + gate-tested work is latent behind a few owner decisions. Added a concise "what each gate unlocks" map so the owner/next session can prioritize by ROI — the **player-damage path** (death-penalty numbers / PvP-consent / hostile NPCs) is the biggest unlock: it makes the entire built wound/medical loop (E2/E3/F7/F8/F31/F32/F9/F17) reachable at once. Surfaces decisions, doesn't pre-decide. Docs-only; full gate GREEN (59 smokes).

**Wave-E follow-up F42 (2026-06-25): test — guard that /help lists every command + channel [PAR, tests].** Closes a real coverage gap: `chat_model_smoke` only spot-checked that `command_help()` contains `/raise` + `/claim` (2 of 7), so adding a command and forgetting /help (the `/who`-in-F27 risk) wouldn't fail the gate. Strengthened it to iterate all of `GAME_COMMANDS` + `CHANNELS` and assert each appears in `command_help()`. Test-only, pure-model. Full gate GREEN (59 smokes).

**Wave-E follow-up F41 (2026-06-25): fix — skill-raise/claim/equip results surface to the GUI status line, not just the console [HOT, client-only].** Completes the result-feedback parity the F39/F40 scan exposed. `_on_skill_raise_replied`/`_on_claim_replied` only `print()`-ed (console, not GUI), so a GUI player who typed `/raise`/`/claim` saw "Raising…/Claiming…" forever and never the result or rejection reason. Added `_set_status` to both branches of skill-raise + claim/release, plus the equip rejection — matching heal/zone/auth. Pure parity, no mechanic. Full gate GREEN (59 smokes). **Two-process (no-regression):** claim + skill-raise handlers both fired (`[territory] claimed n1`, `[skillraise] blaster raised to 0D+1 (cost 3)`); the status lines live in those verified branches.

**Wave-E follow-up F40 (2026-06-25): fix — command-bar commands with a missing arg give a usage hint, not a silent no-op [HOT, client-only].** Same silent-failure class as F39. `/heal` already handled its empty case, but `/raise`/`/travel`/`/claim`/`/release` did nothing (no feedback) when typed without their arg. Added an `else` to each calling a new `_usage(msg)` helper (logs `[cmd] usage: …` + flashes "Usage: …" with an example). Pure UX fix. Full gate GREEN (59 smokes). **Two-process verified:** bare `/raise` logged `[cmd] usage: /raise <skill> (e.g. /raise dodge)`.

**Wave-E follow-up F39 (2026-06-25): fix — /org chat without an org gives feedback, not a silent drop [HOT, client-only].** A reachable silent-failure: the server dropped a no-org `/org` line silently (player saw nothing), and most players have no org yet (faction-join is owner-gated) while `/help` advertises `/org`. `_submit_chat_line` now pre-empts client-side — if channel is `org` and `snapshot.territory.org_id` is empty, it logs `[chat] /org unavailable` + flashes a status and doesn't send. Org members unaffected. Pure UX fix. Full gate GREEN (59 smokes). **Two-process verified:** no-org client logged `/org unavailable` (never hit the server); org member's `/org` still delivered `[Org] …: rega`.

**Wave-E follow-up F38 (2026-06-25): VALIDATION milestone — both persistent-world loops proven end-to-end (docs-only, no mechanic change).** The two core engines of the player-driven world were built but never verified end-to-end (prior two-process runs were far shorter than the 60s resource tick, so treasury always read 0). Drove both with fast ticks: **(1) territory income** — `--resource-tick 0.7`, a Hutt dune_sea claim accrued 150 cr/tick (base 100 × 1.5 lawless) with the org HUD climbing `treasury 0→150→…→1500`; **(2) faction influence** (F37) — kills drove hutt control 58→90 → alert standard→underworld. No bug in either; no code change. The "player-driven persistent world" pillar is now CONFIRMED functional + legible, not just present.

**Wave-E follow-up F37 (2026-06-25): info — surface zone ALERT-level escalations as events [HOT, client-only].** Closes the loop F35 opened. A diagnostic confirmed the persistent-world causal loop works AND is observable: a Hutt player autofiring in the dune_sea drove hutt control 58→90 and the Director escalated alert `standard → underworld` — but that escalation slipped by silently. Now the client logs `[alert] <zone>: <old> → <new>` + flashes a status on an in-zone change; a TRAVEL re-baselines (no false flash). Client-only, no mechanic. Full gate GREEN (59 smokes). **Two-process verified:** flagged `[alert] tatooine.dune_sea: standard -> underworld`. **Validates the full loop end-to-end: player action → faction-control shift (F35) → alert escalation (F37).**

**Wave-E follow-up F36 (2026-06-25): info — show other players' faction ALLEGIANCE on nameplates + /who [HOT].** The people-level companion to F34 (your authority) + F35 (zone control). Player entries showed name+wound (F17) but not which side they're on. The per-peer snapshot now enriches each entry with its `axis` (from `_peer_axes`, org members only); the client appends `[Faction]` to nameplates and the `/who` roster. Pure display, no mechanic; same reachability as F12/F34. Full gate GREEN (59 smokes). **Two-process verified:** a Hutt player's `/who` read `2 players: Greedo [Hutt], Cody [Republic]`.

**Wave-E follow-up F35 (2026-06-25): info — surface the Director's per-zone faction CONTROL in the zone HUD [HOT, client-only].** A "surface invisible state" slice (cf. F9/F12/F29/F34). The Director's per-zone faction influence (`zone.influence`, axis-keyed over republic/cis/hutt/independent — drives alert+security, fed by player actions via E24) was already in every snapshot but unrendered. The zone HUD now appends `control: <top-2 factions>` ("Republic 55 · Hutt 42") and logs `[control] <zone> <axis>=<inf> …` on change. Client-only, pure presentation, no mechanic. Full gate GREEN (59 smokes). **Two-process verified:** spaceport read `republic=55 hutt=42 independent=25 cis=5`, dune_sea read `hutt=60 independent=30 cis=20 republic=10` — each matching the seeded zone data, sorted desc.

**Wave-E follow-up F34 (2026-06-25): info — surface the viewer's faction RANK + territory authority in the org HUD [HOT].** A "surface invisible state" slice (cf. F9/F12/F29). `faction_rank` is stored + persisted and gates territory actions (`can_claim_command` needs rank ≥ RANK_CLAIM 3; found-a-city needs RANK_CITY 5), but was invisible until a rejected claim. Server now caches `_peer_ranks[peer]` and the snapshot `territory` block carries `your_rank` + the thresholds; the org HUD shows `rank N (can claim)`/`(org leader)`/`(need 3 to claim)` and logs `[org] … rank=N`. Pure display, no mechanic. Full gate GREEN (59 smokes). **Two-process verified:** a rank-3 member read `rank=3` (HUD "can claim"), a rank-2 member read `rank=2` (HUD "need 3 to claim").

**Wave-E follow-up F32 (2026-06-25): fix — an incapacitated/dead character can't move either [HOT].** Completes F31's "out" state: F31 gated firing but an incapacitated character could still walk around. WEG: incapacitated/mortally/dead are immobile. `world_state.set_input` gained an optional `can_act` param (false zeroes move+jump, keeps yaw); `network_manager.submit_input` passes `can_act = arena wound < DISABLED_SEVERITY` (fail-open to true when not in arena). Faithful WEG, not a divergence. Added a `[pos]` autowalk readout (reusable headless net-movement test affordance). net_smoke extended (can_act=false → no move; can_act=true → moves). KNOWN: same latency as F31 (nobody reaches incapacitated live yet). Full gate GREEN (59 smokes). **Two-process (no-regression):** a healthy autowalk client still moved forward (`[pos]` Z −15.42 → −54.43).

**Wave-E follow-up F31 (2026-06-25): fix — an incapacitated/dead character can't act.** Combat-correctness audit fix. The wound ladder models incapacitated/mortally/dead penalty as 0D *because* they're out and don't act, but `combat_arena.submit_fire_intent` queued intents regardless — so a downed character (0D penalty) would fire at full strength. Added a guard: drop the fire intent when the shooter's `player_wound_severity >= DISABLED_SEVERITY (3)`; the can-act tiers (sev ≤ 2) fire normally. Faithful WEG, not a divergence. Pure-model fix; combat_arena_smoke extended (sev-3 → 0 intents/envelopes; sev-2 → 1 queued). KNOWN: latent — no player-damage source + `--start-wound` only spawns can-act tiers, so nobody reaches incapacitated in live play yet; correct for when PvP/hostile NPCs land. Full gate GREEN (59 smokes). **Two-process (no-regression):** a healthy autofire still resolved combat (10 hits).

**Wave-E follow-up F30 (2026-06-25): fix — refresh the character sheet on CP award [HOT].** The F24 sheet push fired on register/skill-raise/equip but not on a combat CP award (`_award_cp`), so the sheet panel's CP went stale after kills (the wallet label updated, the sheet didn't). Added `_push_sheet` after the wallet push in `_award_cp`; also logged the wallet CP in the client `[sheet]` line. Pure consistency fix. Full gate GREEN (59 smokes). **Two-process verified:** an autofire client's sheet refreshed `cp=0 → 3 → 6 → … → 24` as each kill awarded CP (pre-F30 it stayed cp=0).

**Wave-E follow-up F29 (2026-06-25): fix — org HUD shows own-org vs total zone claims [HOT, client-only].** The F12 org HUD counted `claims_in_zone.size()` as the player's org claims, but that block carries every org's claims (tagged with org_id) — so "Hutt Cartel · N claims here" wrongly included rival orgs' claims. Now `_update_org` filters by org_id and shows `mine/total` ("1/2 claim(s) here"), accurate and a territory-control glance. Pure presentation, no server change. Full gate GREEN (59 smokes). **Two-process verified (2 org clients):** a hutt player saw 0/0 → (claims n1) 1/1 → (a Republic player claims n2) 1/2 — own count excludes the rival, total includes it.

**Wave-E follow-up F28 (2026-06-25): fix — reset the First-Aid retry gate on full heal [HOT].** Latent correctness bug in the F8/DIV-0013 retry gate (`_heal_treated`): it was never cleared when a wound fully healed, so a target healed to healthy then re-wounded to a previously-treated level (e.g. stunned→healthy, later re-stunned) was wrongly rejected `already_treated`. Fix: `_heal_treated.erase(target)` when `submit_heal` or `_recover_wounds` reaches `healthy`. heal_flow_smoke extended (mirror clears on healthy + a re-wound assertion that fails without the fix). Not ledger-flagged (bug fix). Full gate GREEN (59 smokes). **Two-process:** a medic healed a stunned ally `stunned -> healthy`, exercising the heal-to-healthy erase with no regression.

**Wave-E follow-up F27 (2026-06-25): /help + /who + command discoverability [HOT, client-only].** Closed the command-bar's onboarding gap — F25/F26 added a slash-command system but nothing told the player it existed. Added `/help` (prints `chat_model.command_help()`, the full command + channel list) and `/who` (same-zone roster from the snapshot, with each one's condition via F17), and pointed the chat placeholder at `/help`. Both are client-local (no RPC). chat_model_smoke extended. Full gate GREEN (59 smokes). **Two-process verified:** two same-zone players — A's `/who` logged `[who] 2 players: Jabba, Greedo`. /help is unit-verified.

**Wave-E follow-up F26 (2026-06-25): command bar /claim /release — territory now GUI-playable [HOT, client-only].** Extended the F25 command bar so the territory system is reachable in the GUI (it was headless-only via `--claim`). `chat_model.GAME_COMMANDS` += claim/release; `net_world._dispatch_command` routes `/claim <node>`→send_claim_node, `/release <node>`→send_release_claim (empty-arg guarded). No server change (routes to E23 RPCs). chat_model_smoke extended. Full gate GREEN (59 smokes). **Two-process verified (full arg-command end-to-end):** a no-org client's `/claim n1` (`--say "/claim n1"`, quoted to survive arg-splitting) parsed `{claim,n1}` → send_claim_node("n1") → `[territory] claim n1 rejected (no_org)` — proving arg-commands dispatch over the wire (retroactively confirms F25's /raise//travel).

**Wave-E follow-up F25 (2026-06-25): GUI command bar /raise /travel /heal [HOT, client-only].** Generalized the F22 chat input into a slash-command bar so the GUI reaches the full command surface, not just hardcoded keys — the key fix: the `K` key only raised blaster, but `/raise <skill>` now raises ANY skill (submit_skill_raise already accepted any). New pure `chat_model.parse_command` → `{cmd,arg}` for /raise|/travel|/heal (chat channels + non-commands fall through to chat); `net_world._submit_chat_line` dispatches commands first (raise→send_skill_raise, travel→send_change_zone, heal→send_heal(nearest wounded)) else chat. No server change. Standard slash UI, not a divergence. chat_model_smoke extended. Full gate GREEN (59 smokes). **Two-process verified:** a medic's `/heal` command (`--say "/heal"`) dispatched → First Aid on a wounded ally (`wounded -> stunned`); arg-commands are parser-unit-verified + route to already-verified RPCs.

**Wave-E follow-up F24 (2026-06-25): character sheet view [HOT].** Filled a major missing MMO screen — a player built a character but couldn't see their own attributes/skills/gear in-game. New server→client `apply_sheet(summary)` RPC + `sheet_updated` signal; `_sheet_summary` builds a compact view (species, 6 attributes, non-zero skills, weapon/armor, CP wallet, force-sensitivity); pushed at login and re-pushed on skill-raise + equip success. Client adds a `V`-toggle character-sheet panel + a `[sheet]` log. Pure presentation of the persisted sheet, no new mechanic. RPC surface 20→21. Full gate GREEN (59 smokes). **Two-process verified:** a rodian quickstart got its login sheet (`species=rodian dex=3D+1 weapon=blaster_pistol`) and the re-push after raising blaster (`skills 0→1`). KNOWN: the V-toggle GUI isn't headlessly exercisable; the apply_sheet data path is two-process verified.

**Wave-E follow-up F23 (2026-06-25): in-combat CP/FP pool readout [HOT].** Completed F5's usability gap (F9/F17 surface-invisible-state pattern): F5 lets a player spend in-combat Character Points (C key) and a Force Point (F key), but only the progression wallet was shown — the live combat `player_character_points`/`player_force_points` pool was invisible, so players spent blind. The snapshot "you" block now carries `cp`/`fp` from `arena.player_state`; the client shows a "Boost (C/F): N CP · M FP" HUD (distinct from the wallet) and logs `[boost]` on change. Pure presentation, no new mechanic. Full gate GREEN (59 smokes). **Two-process verified:** an autofire `--fire-cp 2 --fire-fp` client's boost readout depleted `cp=5→3→1→0` then `fp=1→0`. Completes the F5 in-play spend UX.

**Wave-E follow-up F22 (2026-06-25): GUI chat input + slash-command parser [HOT].** Closed a playability gap: chat send was headless-only (`--chat`); a GUI player could read chat but not type one. New pure `chat_model.parse_input(raw)` → `{channel, text}` (`/say /ooc /org /emote` + aliases, plain text → say, unknown `/cmd` kept verbatim, blank → empty). `net_world` adds a HUD LineEdit (Enter opens → movement suppressed while focused; type; Enter sends via `parse_input` → `send_chat`; Esc cancels) + a headless `--say` affordance through the same path. All focus/movement-suppression is gated on `has_focus()` (false in headless), so it can't regress any verified path. Standard chat UI, not a divergence. chat_model_smoke extended (parse_input variants). Full gate GREEN (59 smokes). **Two-process verified:** `--say hello94` round-tripped via parse_input → a same-zone observer saw `[chat] Greedo: hello94`. KNOWN: the LineEdit Enter-to-send flow isn't headlessly exercisable; its parse_input core is gate+two-process verified and the focus plumbing is non-regressing by construction.

**Wave-E follow-up F21 (2026-06-25): chat delivery-scope regression guard.** Added `chat_flow_smoke` — a headless gate guard (claim_flow/auth_flow/heal_flow/zone_flow pattern) for `submit_chat`'s delivery-scope composition (F2 zone + F20 org), previously only two-process verified. Composes the real `chat_model` and locks: validation (whitelist + empty), and scope over a mixed roster — `ooc` → all; `say`/`emote` → same-zone only; `org` → same-org members any zone (incl. cross-zone, excl. no-org/other-org), no-org sender → nobody. Test-only; full gate GREEN (59 smokes). With F21 all five current [HOT] composition chains are gate-guarded (claims F3, auth F4, First Aid F10, zone F14, chat F21).

**Wave-E follow-up F20 (2026-06-25): org chat channel [HOT].** Added a faction-coordination chat channel leveraging the org system (F12). Chat scopes now: `ooc`=global, `say`/`emote`=zone-local, `org`=all connected same-org members in any zone (cross-zone). `chat_model` whitelists `org` + formats `[Org] Name: text`; `submit_chat` routes `org` to peers whose `_peer_orgs` matches the sender (mirrors F2 zone routing, keyed on org), rejecting a no-org sender. No client change (existing `--chat` affordance + shared `format_line`). Standard MMO scoping, not a WEG divergence (like F2). chat_model_smoke extended. Full gate GREEN (58 smokes). **Two-process verified (3 clients):** a hutt member's org msg from the spaceport reached a fellow hutt in the dune_sea (different zone) but not a no-org player in the sender's own zone — org-scoped, cross-zone, members-only.

**Wave-E follow-up F19 (2026-06-25): First Aid targets the nearest wounded ally [HOT, client-only].** Closed the medical loop's last usability gap: the H key / `--heal-other` picked the first other player (often healthy → wasted no_wound), even though F17 put each player's wound in the snapshot. Now `_best_heal_target()` returns the nearest same-zone player whose wound is non-healthy (using F17 wound + F13 pos), and the H key + `--heal-other` use it (H says "no wounded ally nearby" when none). Pure client targeting, no server change. Full gate GREEN (58 smokes). **Two-process verified (3 clients):** with a wounded ally AND a healthy bystander in range, the medic correctly First-Aided the wounded ally (`f19ally: wounded -> stunned`). Completes the loop: F8 heal + F9 own + F17 see-others + F19 target-the-wounded.

**Wave-E follow-up F18 (2026-06-25): melee-weapon combat pools fix; wire derived-stats melee.** Fixed a latent bug + wired the orphaned `derived_stats_model.melee_damage_pool`. `combat_arena._pools_from_sheet` always used the `blaster` skill (ignoring the weapon's real skill) and `parse_pool("STR+3D")` returns 0D, so a melee-armed player dealt zero damage. Now the attack pool uses the equipped weapon's own skill (DEX + skills[weapon.skill]) and `STR+ND` damage resolves as STR + bonus via the derived-stats melee model; ranged weapons unchanged. Pure-model fix (no net-wiring change); not ledger-flagged — for reachable play (blaster starter) behavior is identical. KNOWN: melee weapons aren't acquirable yet (equipment gates to owned items; needs the owner-gated economy), so the fix is latent-but-correct. combat_arena_smoke extended (vibroblade STR 2D+3D=5D; attack DEX 3D + melee_combat 1D=4D). Full gate GREEN (58 smokes). Two-process: equip(vibroblade) rejected `not_owned`; a no-regression autofire run confirmed ranged combat still resolves (8 hits).

**Wave-E follow-up F17 (2026-06-25): other players' condition on nameplates [HOT].** Closed a medical-loop usability gap: First Aid (F8) heals an ally and F9 shows your own condition, but player snapshot entries carried no wound, so you couldn't see who's hurt (medics healed blindly). `_build_snapshot` now enriches each zone-filtered player entry with its live `wound`; the client shows it on each remote player's nameplate ("Name — Condition", tinted, reusing F9 colours) and logs `[nameplate]` on change. WorldState.snapshot() untouched (enrichment is network_manager-side), so wire/snapshot smokes are unaffected. Full gate GREEN (58 smokes). **Two-process verified:** an observer saw a wounded ally's nameplate as "Wounded-Ally is Wounded". Completes the medical loop's usability (F8 heal + F9 own + F17 see-others).

**Wave-E follow-up F16 (2026-06-25): render ambient NPCs [HOT, client-only].** Made E27's ambient sim visible (F9/F12 pattern): the server simulates + broadcasts a per-zone NPC roster (`snapshot.npcs`) but the client only counted them, so the world looked empty. `net_world` now renders each ambient NPC as a muted capsule marker (distinct from saturated player avatars) with a kind label, reconciled each snapshot (spawn/lerp/free); the per-zone roster swaps on travel (F11). Logs `[npc] showing N`. Client-only, no server change (data already existed; not ledger-flagged). Full gate GREEN (58 smokes). **Two-process verified:** the server populated the spaceport with 4 NPCs and the client rendered them (`showing 0` → `showing 4`). The world is now populated.

**Wave-E follow-up F15 (2026-06-25): per-species movement speed [HOT].** Wired the built-but-orphaned `derived_stats_model` (it had no net-layer reference): `world_state` moved all players at a fixed `MOVE_SPEED=6.5`, ignoring each species' WEG Move rate. Documented DIV-0015 first, then `world_state` gained a per-player `move_speed` (default = the 6.5 baseline) + `set_move_speed()`, and `tick()` integrates each player's own speed; at login the server sets `MOVE_SPEED × species_move / DEFAULT_MOVE(10)` via `derived_stats_model.move_for_species`. Movement is server-owned (clients follow snapshots) so no client change. `net_smoke` extended to lock the effect. Full gate GREEN (58 smokes). **Two-process verified (3 species):** wookiee speed=7.15, human 6.50, mon_calamari 5.85 — species now mechanically distinct for movement, baseline preserved. Divergence DIV-0015.

**Wave-E follow-up F14 (2026-06-25): zone subsystem regression guard.** Added `zone_flow_smoke` — a headless gate guard (claim_flow/auth_flow/heal_flow pattern) for the zone subsystem's network_manager composition (F11 travel + F13 visibility), previously only two-process verified. Locks: (1) `submit_change_zone` precedence (unregistered → unknown_zone → already_here → travel, persisting record.zone); (2) register zone resolution (build.zone wins, else record.zone restore, invalid ignored); (3) the same-zone player filter (a viewer sees only same-zone players, incl. self). Test-only; full gate GREEN (58 smokes). With F14 all four current [HOT] composition chains (claims F3, auth F4, First Aid F10, zone F14) are gate-guarded, not just two-process verified.

**Wave-E follow-up F13 (2026-06-25): zone-scoped player visibility [HOT].** Completed the zone model — the per-peer snapshot's player list was still global, so a player in the dune_sea saw avatars of players in the spaceport (different conceptual places sharing the prototype's one Mos Eisley geometry), inconsistent with zone-scoped chat (F2) + travel (F11). `_build_snapshot` now filters the player list to same-zone peers (own entry always kept). Safe — `state.snapshot()` returns a fresh dict/entries per call (verified), so per-peer mutation can't corrupt others. The client's avatar reconciliation now makes a traveler's capsule appear/vanish for others; status reads "players in zone" + logs `[presence] players_here=N`. Standard MMO zone visibility, not a WEG divergence (consistent with F2/DIV-0001). Full gate GREEN (57 smokes; world_state untouched). **Two-process verified (3 clients):** A+C in the spaceport saw players_here=2 while B alone in the dune_sea saw players_here=1. With F13 the zone model is coherent: chat, travel, org/territory view, and presence are all zone-scoped.

**Wave-E follow-up F12 (2026-06-25): org / territory HUD readout [HOT, client-only].** Made the org/territory system visible (F9 pattern): the per-peer `snapshot.territory` block (E23: org_id + treasury + claims_in_zone) was broadcast but the client only read it in the claim-result handler, so an org member couldn't see their org, treasury, or holdings. `net_world` now shows an "Org: <name> · <treasury> cr · <N> claim(s) here" HUD updating each snapshot (blank for no-org) and logs `[org]` on change. Zone-scoped, so it tracks F11 travel. Client-only, no server change (data already existed; not ledger-flagged). Full gate GREEN (57 smokes). **Two-process verified:** a hutt-org player in the dune_sea saw the org readout go claims_here=0 → 1 after claiming node n1.

**Wave-E follow-up F11 (2026-06-25): inter-zone travel [HOT].** Unlocked the multi-zone world — `_peer_zones` was only set at connect/register, so players were locked to their login zone and the whole per-zone stack (snapshot routing, zone-scoped chat, ambient, territory) was only reachable by reconnecting with `--zone`. Documented DIV-0014 first, then a `submit_change_zone(zone_id)` RPC: validates the zone, updates `_peer_zones` (snapshot/chat/ambient/territory all follow), persists `record.zone` (restored on next login), and replies. Command fast-travel to any loaded zone (no adjacency/cost yet). Snapshot carries a cached `zone_list`; client gets a `T` cycle-travel key + `--travel` affordance + logs zone changes. No pure-model change (zone_state untouched). Full gate GREEN (57 smokes). **Two-process verified:** a client traveled spaceport→dune_sea and its snapshot zone followed (`now in Mos Eisley Spaceport District (secured)` → `now in The Dune Sea (lawless)`). Divergence DIV-0014 (command fast-travel vs MUSH spatial exits).

**Wave-E follow-up F10 (2026-06-25): First-Aid precedence regression guard.** Added `heal_flow_smoke` — a headless gate guard (claim_flow/auth_flow replicate-the-wiring pattern) for the `submit_heal` composition precedence (DIV-0013/F8), previously only two-process verified. Composes the real `recovery_model` and locks `self → no_target → out_of_range → no_wound → beyond_help → already_treated → heal`: self/out-of-range/no_wound precede later checks, a big pool walks the ladder down (wounded→stunned→healthy, re-heal allowed once the level changes), and a failed roll trips the per-target retry gate (already_treated). Deterministic via extreme pools + seeded RNG. Test-only; full gate GREEN (57 smokes). With F10 all three [HOT] validation chains (claims F3, auth F4, First Aid F10) are gate-guarded, not just two-process verified.

**Wave-E follow-up F9 (2026-06-25): player condition / wound readout [HOT].** Made the F7/F8 wound system visible — it worked server-side but a player couldn't see their own wound (the snapshot didn't carry it). `_build_snapshot` now folds a per-peer `you = {wound}` block (the player's own live combat wound, mapped to a label) into each snapshot — pure presentation data, no mechanic (not ledger-flagged). The client shows a colour-coded "Condition: …" HUD that updates as the server changes the wound (combat damage, natural recovery, First Aid) and logs `[condition] you=<level>` on change. Pure/presentation split honored. Full gate GREEN (56 smokes). **Two-process verified:** a `--start-wound wounded` client's readout flowed `you=wounded -> stunned -> healthy` as natural recovery healed it. The wound lifecycle (escalate → self-recover → medic First Aid) is now end-to-end visible to players.

**Wave-E follow-up F8 (2026-06-25): First Aid — a medic heals a wounded ally [HOT].** Extends F7 with the Guide_19 §3 medical-aid path. Documented DIV-0013 first, then a `submit_heal(target_id)` RPC: a healer First-Aids another connected player in the same zone (never self); heal pool = the healer's Technical + first_aid skill vs the target's wound difficulty (server RNG); success drops the target's wound one level, persists, and refreshes only the target's live combat penalty. Unlike self-recovery it can treat incapacitated + stabilize mortally_wounded (healthy/dead rejected); no death roll/penalty (owner-gated). A per-target retry gate (cleaned on disconnect) blocks spam-to-success. First cooperative player-targeting (heal, not PvP → non-owner-gated). Client: an H key + `--heal-other` headless affordance + `_first_other_peer()`. Pure math stays in recovery_model. Full gate GREEN (56 smokes). **Two-process verified (2 clients):** a medic First-Aided a stunned ally over the wire (`stunned -> healthy, First Aid 3D rolled 15 vs 8`). Divergence DIV-0013.

**Wave-E follow-up F7 (2026-06-25): wire natural wound recovery into the live loop [HOT].** Closed the persistent wound→recovery loop — the Wave-E `recovery_model` (DIV-0009) had a smoke but was never referenced in the net layer, so a wounded character stayed wounded forever. Documented DIV-0012 first, then `network_manager._recover_wounds()` runs once per Director tick: each connected character at a "can still act" wound tier (stunned/wounded/wounded_twice) makes a natural self-recovery `heal_check` with their own Strength vs the Guide_19 §3 difficulty (server RNG); on success the wound drops one level, persists, and refreshes only the live combat penalty (`set_player_combat` merges just `player_wound_severity`, leaving depleted CP/FP intact). incapacitated/mortally_wounded/dead are excluded (medical / owner-gated death; no death roll auto-run). Added a `--start-wound` headless affordance. Pure math stays in `recovery_model`. Full gate GREEN (56 smokes). **Two-process verified:** a new Strength-3D char started `wounded` and the server healed it `wounded -> stunned -> healthy` over recovery ticks. Divergence DIV-0012 (heal-pool = own Strength + ~30s interval are documented approximations, not owner balance calls).

**Wave-E follow-up F6 (2026-06-25): evict the record cache on disconnect [HOT].** Fixed the unbounded-`_record_cache` leak flagged as a known E26 follow-up: `_on_peer_disconnected` cleaned the five per-peer maps but never evicted the character-id-keyed cache, so on a long-running server every character that ever logged in stayed cached for its lifetime (plus a latent stale-cache risk). Fix captures `character_id` before the map erase, then `_record_cache.erase(character_id)` after `_save_peer` flushes to disk (safe: single-session lock + fresh read-through next login); bounds the cache to connected players. Zone/faction-keyed Director state intentionally untouched. No new model/smoke (network_manager isn't headlessly instantiable; verified two-process). Full gate GREEN (56 smokes). **Two-process verified:** a `leaktest` client registered (cache populated), was killed, and the server logged `[cache] evicted leaktest on disconnect (cache size=0)` + `peer … left` after the ENet timeout.

**Wave-E follow-up F5 (2026-06-25): expose in-play CP/FP spend on the client [HOT].** Connected an already-built, gate-tested, server-validated WEG combat mechanic players could not reach — the client always sent `cp:0, fp:false`, so a Character Point (+1D each) or Force Point (double dice) could never be spent in combat. `net_world.gd` now stages them for the next shot (**C** cycles 0..5 CP, **F** toggles a Force Point; HUD/controls updated), sends them on LMB fire + resets, exposes `--fire-cp N` / `--fire-fp` headless affordances, and surfaces the actual spend per shot in the combat log (`[+NCP]`/`[Force Point]` from the envelope's `attack_cp_spent`/`force_point_spent`). Zero server change (combat_arena already clamped + resolved them); no new model/smoke (resolution covered by ground_combat_model/combat_arena smokes); not ledger-flagged (exposes existing faithful rules). Full gate GREEN (56 smokes). **Two-process verified:** an autofire `--fire-cp 2 --fire-fp` client logged `[+2CP]`, `[+2CP]`, `[+1CP]` (server-clamped to the pool, 5→0), then `[Force Point]` (1→0), then plain shots — client-staged spend resolving server-authoritatively with depletion. (The model spends CP first and reserves the FP until CP is gone — pre-existing, untouched.)

**Doc reconciliation (2026-06-25, zero-risk truth-up).** With the backlog dry, brought the clean-session entry docs in line with the shipped state so the next session orients off facts, not stale targets. `docs/SESSION_HANDOFF.md`: §0 TL;DR now states Wave E (E1–E27) + hardening + F1/F2/F3 COMPLETE, gate at **55 smokes** (was "34"), DIV-0001..0011, and an explicit **backlog-DRY → confirm-green-and-HOLD / needs-owner-steer** status; §1 first-actions reflect Wave E done + the hold-if-dry rule (was "pick the top Wave E batch"); §7 corrects the RPC surface **8 → 16** (enumerated, split client→server `any_peer` / server→client `authority`), adds the Wave E pure models + claim command layer + per-peer state to the key-files map, and flags creatures/vendor/reputation as **modeled+smoked but not yet wired into `net_world`** (economy/spawn-rate/value = owner calls). `docs/MULTIPLAYER_FOUNDATION.md`: header + roadmap "Wave E — IN PROGRESS" → DONE with the full E-tier summary + a "backlog dry / owner-gated next" status. Docs-only (the gate never parses markdown); full gate re-run GREEN (55 smokes + 7 python + import + launch) to confirm the tree is still green. This was the last clearly-valuable non-owner-gated action; the loop holds at the clean state for owner direction.

**(Earlier) Content drop 2 was DONE and the loop stopped at its designed handoff point before the owner chose Wave C.** New (one-way from read-only SW_MUSH): `data/starships_clone_wars.json` (6 era-appropriate civilian craft — GCW/Imperial ships excluded, `content_smoke` asserts none leak in), `data/droids_clone_wars.json` (3 commerce droids), `data/creatures_clone_wars.json` (22 wildlife). The backlog is now dry of unblocked, non-owner-decision, non-visual work. **What remains needs the owner:** a visual check of the low-poly world (then A1b building models + P1 polish), and design decisions on the next headline features — the org/claim command layer + guard NPCs, the **siege/Drop-6D loop** (durations/threshold), **Force/Jedi access & scarcity**, the **death-penalty implementation** (DIV-0006 shape), and whether the **LLM Director** ships at launch. Full gate green throughout (35 GDScript smokes + 7 python).

**Parallel streams integrated (2026-06-24):** content drop 1 — `data/{weg_skill_catalog,species_clone_wars,weapons_clone_wars,armor_clone_wars}.json` (76 skills / 9 species / 32 weapons / 23 armors, ported one-way from read-only SW_MUSH, covered by `content_smoke`); and the persistent world-sim design — `docs/{WORLD_SIM_DESIGN,FACTION_TERRITORY_DESIGN,PERSISTENCE_DESIGN}.md` + `data/schemas/*.json` (Director AI, security zones, the Drop 6D siege state machine, SQLite→Postgres persistence). Gate now 32 GDScript smokes + 7 python; all green.

## Current Playable Slice

- 3D Mos Eisley Spaceport Row / Bay 94 range.
- First-person movement, location and range-target inspection with current/next live behavior explanation, aiming, cover, dodge, full dodge, live staggered remote-pressure volleys, coordinated remote fire-team holds, peeking/tucked covered remotes, near-miss remote pinning, flanking/repositioning remote holds, reload/weapon-cycle remote holds, covering-fire holds, morale-hesitation remote holds, wounded-remote fallback timers, hit suppression, CP, FP, torso-only vest armor soak with visible coverage/quality pips, in-world live remote-state badges, in-world hit-location feedback, persistent body-part damage tinting and scorch/impact marks, stun damage, scale targets, a sine-sweeping B1 range target, a patrol-pattern walker-scale armor target, seeded combat logs with action-window declaration summaries, and live range telemetry. Passive HUD/overlay controls now ignore mouse input, player look/gameplay hotkeys are handled through `_input`, captured-mouse blaster/inspect rays aim from the center reticle, normal dodge moved from `D` to `Q` so WASD movement no longer conflicts with combat declarations, and `Z` pauses/resumes the live remote pressure clock.
- 2.5D Mos Eisley approach mode with opaque modal presentation, shared/tested modal-state detection for CanvasLayer and CanvasItem screens, paused ground controls/range pressure/HUD/character-sheet overlay/target motion, cursor/input capture, a tested responsive viewport-sized tactical plane that rebuilds on viewport changes, model-formatted bridge-mode status with current maneuver difficulty/named-hazard preview, state-aware bridge action buttons with compact maneuver difficulty/hazard-count cues that refresh after piloting maneuvers, model-formatted bridge crew/station strip with commander station, banked-assist requested-alias and station-round visibility, and crew-wound visibility, D6 crew-wound penalties on station actions including ship-aware sensor sweeps/contact identification with wound callouts in action readouts, clickable/selectable contacts, keyboard target cycling, logged target selection, clickable multi-hazard approach zones with collision and non-collision hazard detail/log readouts plus current-maneuver crossing/avoidance preview, clickable station/action controls, selected-contact detail readout with range-aware lock state, WEG scale posture, defensive posture, hull/shield soak posture, weapon posture, crew wound posture, systems posture, model-formatted bridge cue with action hotkeys, movement posture, hostile-fire readiness, counterfire posture, destroyed-weapon-aware and custom data-preserving field-repair options/routing with visible data-driven difficulty labels and yard-only-aware automatic targeting demonstrated on visible traffic, and targeting-penalty preview, capped recent-action log, deterministic sensor sweeps, persistent revealed contacts, per-sweep new-contact callouts, model-formatted contact labels, sensor, contact-identification, comms hail, traffic tick, manual traffic stepping, gunnery action, confidence-modified hidden-target gunnery, gunnery targeting, lock-disruption, counterfire, automatic hostile-fire, ship-condition, shield-reroute, station-assist action, damage-control action with WEG difficulty-name and yard-only callouts, astrogation plot action, maneuver action, assist-replacement, maneuver-hazard, maneuver-collision, crew-wound, and break-lock readouts that show tracks, consumed sensor assists, current known/hidden contact totals, movement, range holds, authoritative current weapon-solution pressure, data-driven successful-hail lock delays and failed-hail lock pressure with ready-lock callouts, ready hostile-fire opportunities, automatic hostile shots, resulting player ship condition, spent/cleared hostile locks, return-fire results, condition countdown changes, and hazard details, persisted margin-based contact confidence, persisted selected-contact identification profiles and comms dispositions with comms lock-delay/pressure context, confidence-annotated gunnery targeting context, combined track/weapon-solution engagement summaries in action and status readouts, confidence-aware lock fallback telemetry, known/hidden contact telemetry, a live traffic clock that advances server-style WEG accounting ticks without player turn advancement, live tactical telemetry with latest automatic hostile-fire summary, banked crew-assist target and station-round visibility with preserved requested target aliases, consumed assist callouts with requested-alias context across player station actions, replacement-aware station assist banking with requested-alias replacement text, helm/fire-control/shield/sensor/communications/engineering/navigation-family station-assist aliases for evasive/weapon-solution/deflector/identification/targeting/hailing/field-repair/route-plot support, commander tactical-coordination assists routed through sensor targeting, and preserved requested assist targets in station audit text, persistent hull-severity status, system-damage flags, deduplicated field-repair counts that exclude yard-only systems, and crew-wound counts, moving/player-tracking contacts, hostile engagement ranges, live hostile fire from ready weapon-solution clocks, blocked-fire stale-lock cleanup for disabled/destroyed hostiles and destroyed player ships, same-tick prior-fire destruction blocking, consumed-lock telemetry cleanup, weapon-solution-gated counterfire with spent-lock telemetry, evasive/gunnery lock disruption, seeded gunnery, shield soak, shield rerouting, rotating crew-station assists, navigator astrogation plotting, data-driven per-system repair difficulty overrides, player-prioritized damage control with formatted before/after condition changes, starship damage/system results, and lightweight ship condition persistence.

## Rules Implemented In Prototype

- Dice pools, pips, Wild Die, Character Point dice, Force Point doubling.
- Ranged difficulty by range band.
- Cover dice and full-cover blocking.
- Aim up to +3D.
- Normal dodge and full dodge.
- Live ground range pressure: active remotes evaluate deterministic incoming-fire windows on a short clock, with target-specific cadence/phase, peeking/tucked, flanking/repositioning, reload/weapon-cycle, covering-fire, morale-hesitation, and fallback metadata staggering which remotes fire on each automatic tick; `V` remains an immediate debug/manual volley from every eligible armed remote.
- Range target return-fire eligibility separates shootable targets from armed combatants, so inert scale/armor drills can be shot without adding bogus remote fire.
- Range target live-fire cadence keeps automatic encounter pressure paced without changing the WEG D6 attack, defense, damage, or soak resolution path.
- Range target suppression metadata lets successful player hits push active remotes off upcoming live-fire ticks without changing WEG attack or damage resolution.
- Range target pinning metadata lets close player misses briefly push configured remotes off upcoming live-fire ticks through a smoke-tested model helper, adding suppressive pressure behavior without changing WEG hit, damage, or soak results.
- Range target peeking metadata lets covered remotes alternate exposed and tucked live ticks, delaying shots as encounter behavior without changing WEG attack, cover, damage, or soak rules.
- Range target flanking metadata lets selected remotes spend configured live ticks repositioning instead of firing, adding NPC pressure variation without changing WEG attack, cover, damage, or soak rules.
- Range target reload metadata lets selected remotes spend configured live ticks cycling weapons instead of firing, adding automatic-fire pacing without changing WEG attack, damage, or soak rules.
- Range target covering-fire metadata lets selected remotes spend configured live ticks applying non-damaging pressure instead of resolving a shot, making live pressure less metronomic without changing WEG attack, damage, or soak rules.
- Range target morale metadata lets selected wounded/stunned remotes spend configured live ticks hesitating instead of firing, adding NPC behavior texture without changing WEG wound penalties, attack, damage, or soak rules.
- Range target coordination metadata lets a ready fire-team pick one shooter per live tick while lower-priority wingmates hold in a `coordinating` state, pacing NPC pressure without changing WEG attack, cover, damage, or soak rules.
- Range target fallback metadata lets remotes that cross a configured wound threshold briefly stop contributing live return fire, adding wounded-NPC behavior without changing WEG wound penalties or attack/damage/soak resolution.
- Ground range telemetry formats the live pressure countdown plus model-derived armed, next-scheduled, suppressed, pinned, currently covered, fallback, coordinating, flanking, reloading, hesitating, and covering return-fire source counts, cover, defense, CP/FP queues, wound state, automatic-volley count, and latest combat audit summary for the HUD, including compact ready/next/suppressed/pinned/covered/fallback/coordinating/flanking/reloading/hesitating/covering pressure hints plus hit-location/armor coverage hints extracted from the latest envelope.
- `range_target_model.gd` now summarizes mixed live remote participant snapshots into ready, waiting, suppressed, pinned, fallback, hesitating, covered, coordinating, flanking, reloading, covering, disabled, inert, and armed counts, keeping Bay 94 telemetry aligned with future server-authoritative encounter accounting.
- `range_state_badge_model.gd` formats the in-world labels over Bay 94 targets and the inspection explanations for those labels, so the current model-derived behavior state is visible without reading HUD telemetry and understandable when inspected.
- Moving range targets use a reusable live motion controller and pure motion model with target-specific sine and patrol/triangle patterns; the B1 remote and walker-scale armor target turn WEG attack/scale resolution into real-time aim/tracking tests, and disabled targets stop moving once WEG damage pushes them to incapacitated severity.
- Range target inspection uses a smoke-tested pure formatter to show profile name, scale, cover, wound state, soak pool, armor coverage/current quality, armed or inert status, weapon pool, and source note when the player presses `E` while looking at a target.
- Range hit feedback uses a smoke-tested pure formatter plus a short-lived in-world marker/label parented to the target, showing hit/miss/blocked state, WEG hit location, armor coverage/degradation, and wound result from the same exchange packet used by telemetry. Successful hits are also recorded on the target and reapplied as persistent body-part material tinting plus small scorch/impact marks until the range drill resets.
- Cached defense rolls for future multi-attacker exchanges.
- Multi-action penalties.
- Wound/stun penalties.
- Armor protection by energy/physical type and Dexterity penalties.
- Armor condition now supports deterministic hit locations, explicit torso-only training blast vest coverage, optional partial-coverage armor soak, and persistent quality-pip degradation when damaging hits penetrate protected armor, feeding later soak rolls without replacing WEG wound or damage bands.
- WEG scale helpers for attack, dodge, damage, and soak.
- Space shields add to hull soak for covered non-ion gunnery hits; ion-tagged weapons bypass shields.
- Starship-shields station action can reroute listed shield dice across front/rear/left/right arcs, with the full reroute action readout formatted and smoke-tested in the status model.
- Crew-station assist actions are data-driven and rotate through pilot, commander, sensors, communications, gunnery, shields, and repair support; each can bank a one-shot bonus for the next matching action, helm/piloting targets such as `helm`, `helm_control`, `evasive`, `break_lock`, `course_correction`, and `approach_vector` normalize to `maneuver`, fire-control targets such as `fire_control`, `target_lock`, `weapon_lock`, `weapon_solution`, `bracketing`, `turret`, and `cannon` normalize to `gunnery`, shield-operation targets such as `shield_reroute`, `starship_shields`, `deflectors`, `deflector_shields`, `angle_shields`, and `shield_timing` normalize to `shields`, sensor-family targets such as `identify`, `identification`, `id`, `targeting`, `targeting_solution`, and `sensor_lock` normalize to `sensors`, communications-family targets such as `hail_contact`, `contact_hail`, `traffic_control`, `transmit`, `broadcast`, and `transponder_ping` normalize to `communications`, engineering-family targets such as `engineering`, `damage_control_repair`, `field_repair`, `patching`, and `systems_repair` normalize to `repair`, navigation-family targets such as `nav_plot`, `route_plot`, `jump_plot`, `jump_calculation`, `hyperdrive_calculation`, `hyperspace_route`, and `local_jump` normalize to `astrogation`, requested target names are preserved in event/banked/consumed-assist packets for audit text when they differ from the normalized target, full station-assist action readouts are smoke-tested status-model text, the live action readout names the consumed assist and requested alias when it is spent, and replacement events record which pending assist was superseded using the same formatter while preserving old/new requested aliases.
- Persisted crew wounds now apply WEG-style station action penalties: stunned/wounded crew reduce matching station pools by 1D, incapacitated or worse crew zero the matching station pool, and the event packets plus smoke-tested status readouts record the station wound context for sensor sweep, contact identification, maneuver, gunnery/defense, shield reroute, damage control, astrogation, communications, and crew-station assist actions.
- Navigator astrogation plot actions roll astrogation/nav pools plus consumed navigator assists against current nav penalties, clear hyperdrive calculation and astrogation difficulty penalties on success, fail cleanly when the hyperdrive is disabled, and use a smoke-tested status-model readout.
- Space sensor sweeps now classify roll margins into missed/faint/partial/solid/clear contact confidence, expose the classification on each sensor event, persist each contact's best-known confidence in tactical state, preserve already revealed contacts after later weak sweeps, report newly revealed contacts per sweep, call out successful tracks, new contacts, consumed assists, and current known/hidden totals through a test-covered status-model readout, show successful track quality in smoke-tested status-model contact labels, annotate gunnery events/readouts with the target's current track confidence through test-covered status-model targeting text, and add WEG-style gunnery difficulty modifiers for hidden or sensor-required targets based on unresolved/faint/partial/solid/clear track quality.
- Contact-identification scans use the selected contact's current sensor confidence as a difficulty modifier, consume banked sensor assists, persist a curated transponder/affiliation/threat profile on success, and show the resulting ID on contact labels without changing gunnery hit math yet.
- Communications hails roll the ship's communications pool against selected-contact identity/threat pressure, consume banked communications assists, persist responsive/unresponsive contact disposition, display data-driven hail responses, and can data-drive a successful-hail delay or failed-hail pressure advance on the selected contact's weapon-solution clock without changing gunnery hit math; pressure packets now flag ready locks when the failed hail completes the hostile firing solution, and comms lock context is persisted into the disposition so contact labels/details keep showing the lock change after the immediate hail readout.
- Space contacts can use data-driven heading movement profiles advanced by a deterministic tactical movement tick; destroyed, drives-disabled, and control-locked contacts now emit explicit movement-block reasons in event packets and traffic readouts, and the overlay drives those ticks on a live five-second clock while keeping `T` as pause/resume for the clock.
- Space contacts can optionally track the player ship during live accounting ticks with a capped turn rate; the Sensor Shadow now uses this as a first hostile behavior profile.
- Player-tracking contacts can hold a configured engagement range once close enough; the Sensor Shadow currently holds at Short range instead of closing forever.
- Armed contacts holding engagement range build stateful weapon-solution clocks with WEG range-band text; ready clocks emit hostile-fire opportunity events and the live overlay can now resolve those into automatic WEG gunnery/damage shots. The Sensor Shadow currently becomes ready after two held-solution ticks.
- Contact movement events now include combined engagement context that summarizes persisted sensor confidence and weapon-solution buildup/ready state for UI and future server telemetry without adding a new combat gate.
- Selected-contact detail now formats WEG scale posture from contact scale data, making cross-scale combat context visible in the side panel before gunnery resolves.
- Selected-contact detail now formats defensive posture from the target's WEG defensive maneuver pool and persistent condition state, reporting controls, maneuverability, drives-offline, controls-dead, or destroyed defense changes in the side panel.
- Selected-contact detail now formats hull/shield soak posture from hull pools, shield pools/arcs, shield loss, and controls-ionized state, reporting target resistance inputs in the side panel without rolling damage.
- Selected-contact detail now formats weapon posture from gunnery pool, fire control, weapon damage, controls-ionized, weapons-offline, and destroyed state, reporting target offensive inputs in the side panel without rolling an attack.
- Selected-contact detail now formats crew wound posture from persisted passenger/gunner damage packets, including stunned crew that can still penalize station actions.
- Selected-contact detail now formats systems posture from Move loss, hyperdrive disabled/calculation state, astrogation difficulty penalties, generator overload, structural damage, and destroyed state, reporting non-weapon/non-defense system state in the side panel without rolling repair.
- Selected-contact detail now formats a bridge cue with action hotkeys from sensor, identification, communications, weapon-solution, sensor-targeting, repairable-condition, and destroyed-state data; the bridge header echoes that next-action cue, colors it by smoke-tested urgency level, and the bridge action row highlights/tints the matching cue buttons from the same structured urgency metadata without adding a new mechanics gate.
- The bridge cue now falls back to local player-ship damage control when no selected-contact cue is more urgent and the local ship has field-repairable damage, matching the damage-control action's player-first repair routing.
- Combined bridge-cue priority is smoke-tested in the status model: destroyed player ship overrides all, selected-contact threats/actions outrank routine local repairs, and local field repair fills otherwise-idle bridge guidance.
- Bridge-cue urgency is smoke-tested in the mode model: abandon-ship cues are critical alerts, destroyed selected-target cues render as neutral status, hostile lock/return-fire cues render with threat wording, local damage-control cues render with repair wording, routine prompts render as next-action guidance, highlighted action buttons carry the matching status metadata plus tooltip wording for consistent tinting, and cue-following player actions get matching compact tags in the recent-action log.
- Space overlay layout now reserves smoke-tested wrapped header and traffic slots above the tactical map, keeping longer bridge cues readable without overlapping the 2.5D play area.
- Selected-contact detail now formats movement posture from the latest movement event, reporting tracking, closing, holding range, patrol movement, or movement blocks in the side panel.
- Selected-contact detail now formats hostile-fire readiness from counterfire, weapon-solution clock, and condition state, reporting acquiring, partial, ready-to-fire, armed, destroyed, or weapons-offline posture in the side panel.
- Successful evasive piloting maneuvers can clear hostile weapon-solution clocks; failed maneuvers keep lock pressure in place.
- Successful player gunnery hits against a locked contact disrupt that target's weapon-solution clock and use a smoke-tested status-model readout for the disruption callout; misses keep lock pressure in place.
- Selected hostile contacts can require a ready weapon-solution clock before counterfiring; the Sensor Shadow now uses this stricter pacing and consumes the ready lock when it fires, reporting the spent lock rounds in the event and a smoke-tested status-model overlay readout.
- Live space accounting ticks combine contact movement with player/contact condition countdowns for ionization, pilot penalties, and control locks, then format movement, range holds, movement blocks, weapon-solution pressure, ready hostile-fire opportunities, automatic hostile shots, resulting player ship condition, and changed conditions through a smoke-tested status-model readout.
- Space tactical telemetry formats live traffic countdown, contact/known/hidden counts, hostile weapon-solution pressure with track confidence from movement engagement context or persisted sensor confidence fallback, latest automatic hostile-fire summary, banked crew-station assists with target actions, requested target aliases when they differ, and station-round age, player ship condition using persistent `worst_hull_severity`/destroyed state, system-damage flags, derived repair counts, crew-wound counts, and automatic update count for the overlay. Current `weapon_solution_counts` is authoritative, so consumed or cleared locks no longer linger through stale movement-event context.
- Space map contact dots and labels are selectable in the modal overlay; the selected contact is highlighted with a smoke-tested detail readout for track quality, ID, comms disposition, weapon-solution lock pressure, sensor-targeting difficulty, and condition, then drives gunnery plus target-side damage-control fallback instead of relying only on the data file's default target.
- Space target cycling is smoke-tested through a pure selection model and wired to `Tab`/`.` for next contact plus `,` for previous contact, so the overlay can be driven without precise mouse clicks; click and keyboard selection now use a smoke-tested formatter, update the scan readout, and append to the recent-action log as `Target` actions.
- Space player-triggered actions now append to a capped recent-action log in the overlay through a smoke-tested pure log model; automatic live traffic ticks stay out of the log to avoid spam, manual `;` traffic steps are logged as deliberate bridge actions, and actions taken through currently highlighted bridge cues carry compact Alert/Threat/Repair/Status/Next tags that are consumed after the first log entry so later target/hazard selections do not inherit stale urgency.
- Space gunnery can derive incoming front/rear/left/right shield arcs from target heading and relative attacker position, with scripted overrides still supported.
- Armed hostile contacts can opt into seeded counterfire after the player's gunnery exchange, and ready live-tick hostile fire uses the same WEG gunnery, scale, shield, hull, system-effect, passenger-damage, crew-wound, and condition-persistence path as player fire. Hostile fire does not consume player-banked station assists; blocked/applied counterfire and automatic hostile-fire summaries are formatted by the status model, including the resulting player ship condition. If a disabled/destroyed hostile or destroyed player ship reaches a ready-fire check, the stale weapon-solution lock is cleared and reported as cleared rather than left in pressure state. If prior automatic fire in the same tick destroys the player ship, later ready hostile shots are blocked as prior-fire destruction and clear their locks.
- Piloting maneuver actions roll piloting plus maneuverability, update heading/position, record WEG movement failures, and now use a smoke-tested status-model action readout.
- Piloting maneuver actions can evaluate data-driven circular approach hazards that increase difficulty and mark obstacle-present collision risk when crossed, with hazard readouts now formatted by the smoke-tested status model, including compact names for multiple crossed hazards.
- Space maneuver hazards are rendered on the tactical map as visible approach zones with difficulty labels; hazard zones are clickable and write a status-model detail readout with piloting modifier, radius, collision-risk wording, and current planned route crossing/avoidance into the scan panel/action log.
- Space gunnery events translate damage margins through WEG starship damage bands; ion hits translate to controls-ionized results; full gunnery action readouts are now smoke-tested status-model text.
- Space gunnery rolls WEG system-effect entries for light/heavy/severe damage.
- Space gunnery returns updated target condition for shield loss, controls ionized, hull severity, system effects, lost Move, disabled weapons/drives, hyperdrive flags, structural damage, and destruction flags.
- Later space gunnery exchanges apply shield loss and controls-ionized penalties to shields, fire control, weapon damage, and defensive maneuver pools.
- Later space gunnery exchanges also apply specific system effects: maneuverability loss reduces defensive maneuver pools, disabled drives remove defensive maneuver, and disabled weapons prevent attacks.
- Controls-ionized condition can count down across ship-condition rounds.
- Damage-control repair actions use WEG repair difficulty bands, can read data-driven per-system difficulty overrides from ship or condition dictionaries, report WEG difficulty names beside the target number, explicitly report field repair unavailable for yard-only systems, report free field-repair time and dockyard credit quotes, can clear one targeted damaged system, and now use a smoke-tested status-model action readout.
- Damage-control events carry before/after condition summaries and consumed crew-assist summaries for server audit logs and live repair-change overlays.
- Live damage-control actions prioritize the local player ship's first field-repairable condition, skip destroyed weapons and yard-only systems, then fall back to the drill target if the local ship has no field-repairable damage.
- Ship condition summaries turn persistent damage state into readable hull status, active flags, deduplicated repairable-system lists, field-repair counts that exclude yard-only systems in compact telemetry, and crew-wound counts for overlays; gunnery, repair, maneuver, and counterfire action readouts now share the smoke-tested status-model condition formatter.
- Passenger/gunner damage events use the WEG character-scale passenger damage chart for light, heavy, severe, and destroyed ship results.
- Named ship crew can roll soak against passenger/gunner damage and persist WEG wound results in ship condition.
- Failed piloting maneuvers use WEG movement-failure bands; collision-band failures become wild spins in open space or speed-based hull collisions when obstacles are present, with collision, crew-wound, and broken-lock readouts now smoke-tested in the status model.
- Action-window initiative/declaration/resolution scaffold.
- Live range exchanges now use `range_action_window_model.gd` to summarize player/remote declarations and assemble ready-for-resolution action-window packets; `ground_combat_model.gd` exposes packet-driven resolution entry points that validate those packets before resolving WEG dice; `combat_event_envelope_model.gd` packages resolved results into server-style event envelopes with optional encounter-state snapshots; `combat_event_log_model.gd` keeps a capped local audit history for future headless-server logging.
- Bay 94 attaches `range_pressure` encounter snapshots to shot and incoming-fire envelopes, including current and next live remote-state summaries, so audit logs can explain what pressure existed when a WEG action window resolved.
- `combat_event_log_model.gd` now extracts the latest `range_pressure` ready/next/suppressed/pinned/covered/fallback/coordinating/flanking/reloading/hesitating/covering counts and latest hit-location/armor coverage result into its compact summary, and `range_status_model.gd` shows that in the HUD audit suffix.
- Character-sheet model for WEG attributes, skill bonuses, gear lookup, CP, FP, wounds, and combat-pool extraction.
- Character sheet overlay toggled with `H`, including live range CP/FP/wound/cover/defense state plus armor coverage and current armor-quality pips.

## Validation

Run from the project root:

```powershell
$machinePath = [Environment]::GetEnvironmentVariable('Path', 'Machine')
$userPath = [Environment]::GetEnvironmentVariable('Path', 'User')
$env:Path = @($machinePath, $userPath) -join ';'
.\tools\check_project.ps1
```

Latest validation in this session passed all checks after improving maneuver action readouts so multi-hazard routes name the first crossed hazards in compact status text.

## Next Best Slices

- Add richer moving-target and remote-fire encounter behavior beyond sine/patrol movement, cadence/phase firing, model-derived state summaries, peeking/tucked cycles, flanking holds, reload/weapon-cycle holds, covering-fire holds, morale hesitation, wounded fallback, and hit suppression.
- Expand the space drill into broader crew-station action variety, richer hazard consequences, target behavior, encounter pacing, and deeper confidence-informed targeting.
- Expand armor UI beyond the range HUD/target inspection/hit markers/body-part tinting, such as richer scorch decals or armor plate breakup.
- Add NPC/remote behavior states such as flanking, morale, and coordinated suppression.

## Standing Constraints

- Keep `C:\SW_MUSH` read-only.
- Keep Clone Wars-only era framing.
- WEG R&E leads mechanics; SW_MUSH code/data is a reference when faithful or useful.
- Prefer free/open tooling and cheap local validation.
